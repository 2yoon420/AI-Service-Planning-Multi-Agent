# LLM API 클라이언트
google-generativeai>=0.8.0
anthropic>=0.40.0

# 오케스트레이션
langgraph>=0.2.0
langchain-core>=0.3.0

# Router+interrupt() 체크포인터 (2026-07-24 추가) — orchestrator/graph.py의
# SqliteSaver.from_conn_string()에 필요. langgraph 본체에는 포함되어 있지 않음.
langgraph-checkpoint-sqlite>=2.0.0

# 환경설정 및 데이터 모델
python-dotenv>=1.0.0
pydantic>=2.0.0

# Upstage는 OpenAI SDK와 호환되는 REST 인터페이스를 제공 (base_url만 교체)
openai>=1.52.0
requests>=2.31.0

# RAG 벡터DB (경량, 로컬 파일 기반)
chromadb>=0.5.0

# 시장조사 에이전트 웹검색 tool (API 키 불필요)
ddgs>=9.0.0

# Writer 에이전트 DOCX 내보내기 (체크포인트3: KOSENA 템플릿 제출 형식 PDF/DOCX, A4)
python-docx>=1.1.0
