# 시장조사 → PESTEL → 경쟁사 비교표 → 기획서 파이프라인 정리

작성일: 2026-07-20

이 문서는 "주제 입력 시 시장조사·PESTEL·경쟁사 비교표·기획서 초안을 단계적으로 생성하는 Multi-Agent" 구현을 위해 조사한 참고 논문, 실제 배포된 서비스, 그리고 실무 표준 파이프라인을 정리한 자료입니다.

---

## 1. 참고 논문

전체 파이프라인(시장조사 → PESTEL → 경쟁사 비교 → 기획서)을 통째로 다루는 단일 논문은 없으며, 아래 논문들을 구성 요소별로 조합해서 참고하는 것을 권장합니다.

### 1-1. 전체 파이프라인에 가장 근접한 논문

- **MaRGen: Multi-Agent LLM Approach for Self-Directed Market Research and Analysis**
  Researcher / Reviewer / Writer / Retriever 4개 에이전트가 협업하여 데이터 조사 → 인사이트 도출 → 시각화 → 최종 리포트 작성까지 end-to-end로 처리. Amazon 컨설턴트 실제 자료로 in-context learning. 6페이지 리포트를 7분, 약 1달러 비용으로 생성.
  https://arxiv.org/abs/2508.01370

### 1-2. 경쟁사 비교표 관련

- **Language Models Guidance with Multi-Aspect-Cueing: A Case Study for Competitor Analysis**
  경쟁사 분석을 위한 LLM 가이던스(Multi-Aspect-Cueing) 기법.
  https://arxiv.org/pdf/2504.02984

### 1-3. 기획서(전략) 초안 생성 관련

- **Multi-Agent Reasoning with Large Language Models for Effective Corporate Planning (SocraPlan)**
  시장조사, 고객 프로파일링, 경쟁 분석, 트렌드 예측을 각각 담당하는 멀티에이전트 구조로 기업 기획서를 작성.
  https://www.researchgate.net/publication/374945016_Multi-Agent_Reasoning_with_Large_Language_Models_for_Effective_Corporate_Planning

### 1-4. 소비자/마케팅 시뮬레이션 관련

- **LLM-Based Multi-Agent System for Simulating and Analyzing Marketing and Consumer Behavior**
  https://arxiv.org/abs/2510.18155

### 1-5. 시장 경쟁 동학(가격 경쟁, 브랜드 충성도 등) 시뮬레이션

- **A Survey on LLM-based Multi-Agent System: Recent Advances and New Frontiers in Application** (CompeteAI 관련 내용 포함)
  https://arxiv.org/html/2412.17481v2

### 1-6. 멀티에이전트 아키텍처 일반론 (설계 참고용)

- **LLM-based Multi-Agent Systems: Techniques and Business Perspectives**
  https://arxiv.org/abs/2411.14033

- **Beyond Self-Talk: A Communication-Centric Survey of LLM-Based Multi-Agent Systems**
  https://arxiv.org/pdf/2502.14321

### 참고: 구현 설계 권장 방향
MaRGen의 4-에이전트 구조(Researcher-Reviewer-Writer-Retriever)를 기본 골격으로 잡고, PESTEL·경쟁사 비교표 부분은 SocraPlan과 Competitor Analysis 논문의 에이전트 역할 분리 방식을 참고하여 결합하는 것을 권장.

---

## 2. 실제 배포된 서비스

| 서비스 | 특징 | URL |
|---|---|---|
| Manus | 범용 AI 에이전트, "Market Research Tool" 플레이북으로 시장조사·경쟁사 분석 자동화 (멀티에이전트 오케스트레이션 기반) | https://manus.im/playbook/market-research-tool |
| StartupShortcut | TAM/SAM/SOM, 경쟁 구도, 타깃 고객, 수요 검증, 리스크 평가 등 7개 섹션 구조 리포트 생성 (월 $59) | https://startupshortcut.com |
| Upmetrics | 2019년 설립, 사업계획서 작성 대표 SaaS. AI 기반 기획서 초안 + 재무 예측 + 피치덱 생성 | https://upmetrics.co |
| LivePlan | 기존 사업계획서 작성 툴에 AI 시장조사 기능 추가 | https://www.liveplan.com |
| Validate My SaaS | 경쟁사 분석(기능·가격·포지셔닝 비교표)에 특화, 실시간 데이터 반영 | https://www.validatemysaas.com |
| IdeaProof | 120초 내 아이디어 검증 + 시장분석 리포트 생성 | https://ideaproof.io |
| CB Insights / PrometAI | 시장/경쟁사 데이터 분석 플랫폼에 AI 리포트 생성 기능 결합 | - |

**평가**: "주제 입력 → 시장조사 → PESTEL → 경쟁사 비교표 → 기획서 초안"을 그대로 단계적으로 노출하는 서비스는 아직 없음. Manus와 StartupShortcut이 구조적으로 가장 근접하고, 나머지는 기획서 작성 쪽에 무게가 실려 시장조사·PESTEL 부분이 상대적으로 얕음.

---

## 3. 실무 표준 파이프라인

### 전체 흐름

```
시장 배경 및 규모 → 거시환경 분석(PESTEL) → 경쟁 환경(비교표 + 포지셔닝) → 기회 요인 및 전략 방향 → 실행 계획
```

### 3-1. 시장조사 (Market Research)

**자료 출처 (어디에서)**
- 1차 자료: 고객 인터뷰, 설문조사, 업계 관계자 미팅, 전시회 현장 조사 (신뢰도 최상, 비용·시간 큼)
- 2차 자료: 통계청, KOTRA, 한국은행 경제통계시스템, 산업연구원(KIET), 산업 협회, Statista, IBISWorld, Gartner, McKinsey/BCG 리포트, 상장기업 IR/사업보고서
- 3차 자료: 뉴스, 블로그, SNS 트렌드, 특허 검색(키프리스, Google Patents) — 최신 트렌드 포착에 유용하나 신뢰도 낮음

**진행 방법 (어떻게)**
1. 리서치 질문 3~5개로 사전 정의 (시장 규모/성장률/고객/타이밍)
2. Top-down + Bottom-up 병행 추정
3. 자료 간 삼각검증(Triangulation) — 최소 2개 이상 독립 출처로 교차 확인
4. 최근 3~5년 시계열로 정리

**판단 기준 (무엇을 기준으로)**
- 출처 신뢰도 (이해관계자 발행 자료는 가중치 낮춤)
- 최신성 (통상 2년 이내 우선)
- 사업과의 관련성 (지역/세그먼트 일치 여부)
- 정량-정성 균형 (숫자와 배경 설명 모두 확보 → 이후 PESTEL 원재료로 활용)

### 3-2. TAM/SAM/SOM 계산

- **TAM** (Total Addressable Market): 카테고리 전체 이론상 최대 시장
- **SAM** (Serviceable Addressable Market): TAM 중 우리 사업 모델이 실제 서비스 가능한 범위 (지역·법규·채널·세그먼트 제약)
- **SOM** (Serviceable Obtainable Market): SAM 중 현실적으로 확보 가능한 점유율

**Top-down**: TAM(산업 리포트) → SAM(지역·세그먼트 비중 곱하기) → SOM(획득 가능 점유율 곱하기)
**Bottom-up**: SOM(영업 역량 기반 역산: 인력 수 × 계약 건수 × 단가) → SAM(잠재 고객 수 × 단가) → TAM(비중으로 역산)
**교차검증**: 두 방식 결과가 비슷한 자릿수면 신뢰, 10배 이상 차이나면 가정 오류 → 보수적 값(주로 Bottom-up) 채택

**자주 하는 실수**: TAM에 무관한 인접 시장 합산, SAM에 제약조건 없이 TAM과 동일 처리, SOM 점유율 근거 없이 낙관적으로 설정

### 3-3. PESTEL 구조화 (정성 정보 → 거시환경 프레임)

1. **Fact 단위 분해**: 원자료(뉴스, 리포트 문단)를 사실 하나당 한 줄로 쪼갬
2. **6축 태깅**: Political(정책/제도) / Economic(거시지표) / Social(인구·가치관) / Technological(신기술) / Environmental(기후·자원 규제) / Legal(산업법·인증) — 경계 케이스는 중복 태깅 허용
3. **임팩트 방향·시급성 판정**: 각 fact마다 기회(+)/위협(-)/중립, 즉시/단기/장기 표기
4. **축별 내러티브 압축**: 같은 축의 fact들을 하나의 요약 문단으로 압축 (기획서에 들어갈 실제 텍스트)
5. **Materiality Filter**: 사업 의사결정에 실제 영향 주는 요인만 남기고 나머지는 축약·삭제

| 축 | Fact | 방향 | 시급성 |
|---|---|---|---|
| Political | 개인정보 가이드라인 강화 | 위협 | 즉시 |
| Political/Economic | 스타트업 세제 혜택 확대 | 기회 | 단기 |

*(자동화 설계 참고: 2~3단계는 LLM이 잘하는 구간, 5단계 중요도 필터링은 도메인 지식 필요 → 사람 검수 권장)*

### 3-4. 경쟁사 비교표 작성

1. 경쟁사 리스트업: 직접 경쟁자 / 간접 경쟁자(대체재) / 잠재 진입자 3그룹 분류
2. 비교축 설정: 가격, 핵심 기능, 타깃 고객, 채널, 투자유치/매출 규모, 강점·약점
3. 매트릭스 표 작성: 경쟁사(행) × 비교축(열)
4. 요약: SWOT 또는 2×2 포지셔닝 맵(예: 가격 vs 품질)으로 인사이트 압축

### 3-5. 기획서 초안 연결

시장조사 요약 → PESTEL 표 → 경쟁사 비교표를 각각 기획서 챕터로 배치. "시장이 크다 → 지금이 타이밍이다(거시환경) → 경쟁사 대비 승산이 있다 → 이런 전략을 편다"는 논리 흐름이 끊기지 않아야 설득력이 유지됨.

---

## 4. 멀티에이전트 구현 시 참고 매핑

| 파이프라인 단계 | 담당 에이전트(안) | 참고 논문/서비스 |
|---|---|---|
| 시장조사 (자료수집·TAM/SAM/SOM) | Researcher / Retriever | MaRGen, Manus |
| PESTEL 구조화 | Analyst 에이전트 (태깅·판정) | SocraPlan |
| 경쟁사 비교표 | Competitor Analysis 에이전트 | Multi-Aspect-Cueing 논문, Validate My SaaS |
| 기획서 작성 | Writer / Reviewer | MaRGen, Upmetrics |

---

## 5. 검증·출처 에이전트의 1~5점 채점 기준 — 근거 검토

PRD에 명시된 "LLM-as-judge로 사실일치도 1~5점 채점(3점 미만=불일치)" 기준의 출처를 추적해본 결과를 정리한다.

### 5-1. 확인한 문서와 결과

| 확인 문서 | 결과 |
|---|---|
| `AI서비스기획보조_MultiAgent_PRD.docx` | "1~5점, 3점 미만=불일치"라고만 명시, 근거 설명 없음 |
| `AI서비스기획보조_MultiAgent_프로젝트기획안.docx` (v6) | 채점 척도에 대한 언급 없음 |
| `26_SUMMER_project.pdf` (과제 원본 명세서) | "완성도·근거 충실도 평가 기준을 함께 합의함"이라고만 되어 있고 구체적 척도 미지정 |
| TTAK.KO-11.0280-Part1/R1 (AI 신뢰성 평가방법 표준) | 검증데이터셋 충실도 기반 "AI Reliability 지수" 개념만 정의, 1~5점 척도 규정 없음 |

**결론**: 참조 문서 어디에도 "1~5점"이라는 구체적 척도를 지정한 근거가 없음. PRD 작성자(본인)가 임의로 설정한 값으로 판단됨.

### 5-2. 부수적으로 발견한 불일치

PRD에 "3점 미만(70% 미만)"이라는 표현이 병기되어 있으나, 5점 만점 기준 3점은 산술적으로 60%(3/5)이며 70%와 정확히 대응하지 않는다. 두 임계값이 수식으로 연동된 것이 아니라, "대략 3점 미만 = 대략 70% 미만"이라는 느슨한 병기로 추정된다.

### 5-3. 그럼에도 완전히 근거 없는 임의는 아닌 이유

1~5점(Likert 5점) 척도는 LLM-as-judge 연구에서 흔히 쓰이는 관행이다. 아래 문헌으로 보강 가능:

- **G-Eval: NLG Evaluation using GPT-4 with Better Human Alignment** (Liu et al., 2023) — Chain-of-Thought + Form-filling 방식으로 LLM이 Likert 척도 점수를 매기는 방법론을 제시, 사람 평가와의 정렬도(Spearman correlation 0.514)를 검증함.
  https://arxiv.org/abs/2303.16634

- **Grading Scale Impact on LLM-as-a-Judge: Human-LLM Alignment Is Highest on 0-5 Grading Scale** — 여러 채점 척도(이진, 0-5, 0-10, 0-100 등)를 비교했을 때 0-5 척도에서 사람-LLM 정렬도가 가장 높다는 실증 결과를 제시.
  https://arxiv.org/html/2601.03444v1

### 5-4. 권장 조치

PRD의 "1~5점, 3점 미만=불일치" 조항에, 위 두 논문(특히 Grading Scale Impact 논문)을 인용해 "0-5 척도가 LLM-as-judge에서 사람과의 정렬도가 가장 높다는 실증 근거를 채택함"이라는 한 줄을 추가하면, 근거 없이 떠 있던 설계값이 문헌적으로 뒷받침되는 결정으로 바뀐다. "3점 미만(70% 미만)" 병기는 둘 중 하나로 통일하거나, 70% 기준의 별도 산출 근거(예: 3.5점/5점=70%)를 명시하는 것을 권장.

---

## 6. 모델/인프라 선택 — 코드 구조·현재 상태·한계·보강 방향 (2026-07-20)

Day 1~3 작업에서 실제로 만든 코드를 기준으로, 각 기술 선택의 현재 구조와 한계, 앞으로 보강할 방향을 정리한다. (발표용 Q&A가 아니라, 코드 유지보수·확장을 위한 기록 목적)

### 6-1. LLM 공급자 (Gemini / Claude / Upstage)

- **코드 구조**: `.env`에 `GOOGLE_API_KEY`/`UPSTAGE_API_KEY`/`ANTHROPIC_API_KEY`를 관리. 각 파일(`rag/build_corpus.py`, `rag/query.py`, `agents/market_research.py`)마다 `get_upstage_client()` / `get_client()`라는 이름으로 `OpenAI(api_key=..., base_url="https://api.upstage.ai/v1")`를 생성하는 함수가 개별적으로 중복 정의되어 있음.
- **현재 상태**: Upstage(Solar Pro 2/3)만 실사용 중. Gemini는 billing 미연동으로 quota 0(429 에러 확인), Claude는 체험 크레딧 미발급 상태.
- **한계**: (1) 공급자 전환(fallback) 로직 없음 — Upstage 장애 시 전체 파이프라인이 멈춤. (2) client 생성 함수가 파일마다 중복돼 있어, API 방식이 바뀌면 여러 곳을 고쳐야 함.
- **보강 방향**: 공용 `llm_client.py` 모듈로 client 생성 로직을 한 곳에 모아 중복 제거. 다중 공급자 fallback은 MVP 이후 과제로 유보.

### 6-2. SQLite / Chroma 선택

- **코드 구조**: `fact_store/store.py`(SQLite, 파일 하나로 동작), `rag/build_corpus.py`+`rag/query.py`(Chroma `PersistentClient`, `rag/chroma_db/` 폴더에 파일 저장).
- **현재 상태**: 로컬 디스크에서는 정상 동작 확인(disk I/O 문제 없음). 이 리모트 테스트 환경(클라우드 마운트 폴더)에서는 SQLite가 disk I/O error를 낸 적 있음.
- **한계**: 동시 다중 사용자 미지원. 클라우드 동기화 폴더(iCloud 등)로 프로젝트 위치가 바뀌면 같은 문제가 재현될 수 있음.
- **보강 방향**: 실서비스로 확장 시 PostgreSQL/전용 벡터DB(Qdrant 등)로 마이그레이션이 필요하다는 점을 PRD 비기능요구사항에 명시.

---

## 7. RAG 코퍼스 구축 — 코드 구조·현재 상태·한계·보강 방향 (2026-07-20)

### 7-1. 파이프라인 구조와 용도

- **코드 구조**: `rag/parse_documents.py`(PDF→Markdown, sync 413 발생 시 async 자동 전환) → `rag/build_corpus.py`(청크 분할+임베딩+Chroma 저장) → `rag/query.py`(검색).
- **현재 상태**: TTA 4건(131청크) + NCS 9건(1,130청크), 총 1,261개 청크 적재 완료. 이 코퍼스는 시장조사·PESTEL의 도메인 데이터가 아니라 검증·출처 에이전트의 메타데이터/저작권 스키마 및 오케스트레이터 설계 방법론 근거 자료임.
- **한계**: 실제 시장 데이터는 이 코퍼스에 없고 시장조사 에이전트의 웹검색 결과(Fact Store)에서 나온다는 구분을, 문서화하지 않으면 헷갈리기 쉬움.
- **보강 방향**: 아키텍처 문서·다이어그램에 "RAG=방법론 근거", "웹검색→Fact Store=시장 데이터"라는 역할 구분을 시각적으로 명시.

### 7-2. 청크 분할 파라미터

- **코드 구조**: `CHUNK_SIZE=800`자, `CHUNK_OVERLAP=100`자 (`build_corpus.py` 상단 상수로 정의).
- **현재 상태**: 실험(ablation)으로 튜닝한 값이 아니라 실무적 근사치.
- **한계**: 근거 부족.
- **보강 방향**: 시간이 되면 청크 크기별(400/800/1200자) 검색 정확도 비교 실험을 1회라도 추가.

### 7-3. 목차(TOC) 노이즈 필터링

- **코드 구조**: `clean_markdown()`(점선 리더·페이지번호 줄 제거) + `_content_density()`(한글/영문/숫자 비율 40% 미만 청크 제거), 둘 다 `build_corpus.py`에 구현.
- **현재 상태**: TTA 기준 146→131개 청크로 노이즈 감소 확인(실제 검증됨). "제목+페이지번호가 한 줄에 붙은" 케이스는 여전히 일부 통과.
- **한계**: 완전하지 않음(의도된 트레이드오프 — 검증 에이전트가 상위 1~2개 결과만 우선 참고하는 구조라 실사용 영향은 제한적이라 판단).
- **보강 방향**: 필요 시 `PAGE_NUMBER_LINE_PATTERN`을 줄 전체뿐 아니라 줄 끝에 붙은 페이지번호까지 잡도록 정규식 보강 가능(현재는 우선순위상 보류).

---

## 8. 검증 기준·에이전트 품질 — 코드 구조·현재 상태·한계·보강 방향 (2026-07-20)

### 8-1. 1~5점 채점 척도 근거 (5장 내용 연결)

- **현재 상태**: 5장에서 정리한 대로, PRD의 "1~5점, 3점 미만=불일치" 척도는 문헌적 근거(G-Eval, Grading Scale Impact 논문) 없이 설정된 값이었음을 확인. "3점 미만"과 "70% 미만"이 산술적으로 불일치(3/5=60%)한 것도 발견함.
- **한계**: 검증·출처 에이전트가 아직 구현 전이라 이 척도가 실제 코드에 적용된 적은 없음. PRD 문서 자체에는 아직 논문 인용 미반영.
- **보강 방향**: PRD에 두 논문 인용 반영, 3점/70% 표기 통일. 검증·출처 에이전트 구현 시 이 척도를 그대로 가져다 씀.

### 8-2. TAM/SAM/SOM Top-down/Bottom-up 불일치

- **코드 구조**: `agents/market_research.py`의 `_estimate_topdown()`(LLM이 fact에서 TAM 수치·SAM/SOM 비중을 추출·제안) + `calculate_market_sizing()`(코드가 실제 곱셈 계산, Top-down/Bottom-up 비교, `discrepancy_flag` 판정). LLM은 숫자 해석과 비율 제안까지만 하고, 실제 산술은 코드가 전담하도록 설계함(LLM 계산 오류 방지).
- **현재 상태**: 구현 완료. 실행 결과 Top-down은 정상 계산됨(TAM 84.4억 USD → SAM 25.3억 → SOM 1.27억). Bottom-up은 사업모델 파라미터(잠재고객 수·단가·목표확보율)가 없으면 자동으로 생략되고 "데이터 부족"으로 명시됨 — 실제로 파라미터를 넣은 실행에서는 정상 계산됨.
- **한계**: PRD가 언급한 "10배 차이" 사례는 아직 동일 조건에서 재현해보지 않음(Top-down과 Bottom-up이 같은 실행에서 함께 나온 적이 없었음 — 한쪽은 TAM 못 찾음, 다른 쪽은 Bottom-up 파라미터 없음).
- **보강 방향**: Top-down·Bottom-up이 둘 다 계산되는 케이스로 한 번 더 실행해, `discrepancy_flag` 로직이 실제로 발동하는지 검증할 것.

### 8-2-1. Top-down 비율 근거의 출처 조작(hallucination) 발견 (Day 3, 2026-07-20 발견·수정)

- **현재 상태(발견 당시)**: `_estimate_topdown()` 실행 결과, SAM/SOM 비중의 근거 설명에 "(출처: Statista 2023 시니어 헬스케어 트렌드 보고서)", "(근거: 2022년 CB Insights 웨어러블 시장 진입 장벽 분석)"처럼 실제 수집된 fact 목록에는 전혀 없는 리서치 기관명·보고서명이 등장함. LLM이 그럴듯한 가짜 출처를 스스로 지어낸 것으로 확인됨. 이는 PRD 문제정의(2-1절, "출처 미표기·사실 왜곡 위험")가 실제로 발현된 사례임.
- **수정 내역**: (1) 프롬프트에 "fact 목록에 없는 리서치 기관명·보고서명을 지어내지 말 것, 근거가 없으면 반드시 '(업계 일반적 추정치, 별도 출처 없음)'이라고 명시할 것"을 명시적으로 추가. (2) `KNOWN_RESEARCH_FIRMS` 목록과 `_flag_fabricated_citations()` 함수로, fact 목록에 없는 유명 리서치 기관명이 근거 설명에 등장하면 경고 문구를 자동으로 덧붙이는 후처리 검증을 추가(완전 자동 차단이 아니라 사람이 알아채기 쉽게 표시하는 수준).
- **한계**: (1) 프롬프트 지시만으로는 재발 방지가 100% 보장되지 않음(LLM은 지시를 무시하고 다시 지어낼 수 있음). (2) `KNOWN_RESEARCH_FIRMS` 목록에 없는 가짜 출처(예: 목록에 없는 군소 기관명이나 존재하지 않는 학술지명)는 여전히 못 걸러냄 — 이건 완전한 해결책이 아니라 완화책(mitigation)임.
- **보강 방향**: 검증·출처 에이전트 구현 시, "이 문장이 인용하는 출처가 실제 Fact Store/RAG 코퍼스에 존재하는가"를 프로그램적으로 대조하는 로직(예: 문자열 매칭 또는 임베딩 유사도 기반 검증)을 반드시 포함할 것. 지금의 이름 목록 방식은 임시방편이며, 근본적으로는 "LLM이 언급한 모든 출처는 실제 근거 DB에서 검색 가능해야 한다"는 규칙을 시스템 차원에서 강제해야 함.
- **재실행 검증(2026-07-20)**: 수정 후 재실행 결과, SAM/SOM 근거가 실제 fact 2개를 조합해 계산 과정을 보여주고("북미 1,052.8억 vs 글로벌 5,052.8억 = 20.8%"), 근거 부족 부분은 "별도 출처 없음"/"업계 일반적 추정치, 별도 출처 없음"으로 정직하게 표기됨. `⚠️ 자동검증 경고`도 발동하지 않음(가짜 기관명 미발생). 다만 이는 이번 1회 실행에서 재발하지 않았다는 확인일 뿐, 위 "한계" 항목(패턴 목록 기반 완화책의 근본적 한계)은 여전히 유효함.

### 8-3. 시장조사 에이전트의 "정보 없음" 메타 발언 저장 버그 (Day 3, 2026-07-20 발견·수정)

- **코드 구조**: `agents/market_research.py`의 `extract_facts()` 함수. Solar Pro 2에게 검색 스니펫에서 fact를 구조화 출력(JSON, `FACTS_SCHEMA`)으로 뽑게 함.
- **현재 상태(버그 발견 당시)**: 첫 실행에서, 모델이 "~에 대한 구체적인 정보는 포함되어 있지 않습니다" 같은 정보 부재 설명 문장을 fact처럼 채워 반환 → Fact Store에 실제로 저장되는 문제 발생. 프롬프트에 "쓸만한 내용 없으면 빈 배열 반환"이라 지시했음에도 발생.
- **수정 내역**: (1) 프롬프트에 "정보 부재를 설명하는 문장 자체를 fact로 넣지 말라"는 지시를 구체적으로 추가. (2) `NO_INFO_PATTERNS` 정규식 목록과 `_is_no_info_statement()` 함수로 코드 레벨에서 한 번 더 필터링(이중 안전장치). 재실행 결과 메타 발언 없이 정상 fact만 저장됨을 확인.
- **한계**: `NO_INFO_PATTERNS`는 지금까지 관찰된 표현만 하드코딩한 목록이라, 다른 표현("데이터를 확인할 수 없음" 등)은 못 걸러낼 수 있음.
- **보강 방향**: 패턴 목록을 운영하면서 계속 추가하거나, 별도의 짧은 LLM 판정 단계("이 문장이 실제 사실인가, 정보 부재 발언인가")를 추가하는 방법도 고려 가능(비용·속도 트레이드오프 있음).

### 8-4. Fact 간 상호 모순 미검증 (실제 관찰 사례: 35% vs 45.7%)

- **현재 상태**: 같은 세션에서 "북미 웨어러블 의료기기 시장 점유율"에 대해 서로 다른 두 수치(35%, 45.7%)가 각각 별도 fact로 저장된 것을 실제로 확인함. 현재 시장조사 에이전트는 이 모순을 감지하지 않고 그대로 둘 다 저장함.
- **한계**: 설계상 이 역할(교차검증·모순 탐지)은 검증·출처 에이전트가 맡기로 되어 있으나 아직 미구현.
- **보강 방향**: 검증·출처 에이전트 구현 시, 같은 `topic_relevance`(리서치 질문)를 공유하는 fact들끼리 수치 모순 여부를 대조하는 로직을 반드시 포함할 것.

### 8-5. 1인 개발·2주 일정

- **현재 상태**: MoSCoW로 Must 항목만 방어하는 방식으로 범위를 압축. Day 1~3 동안 413 오류(대용량 문서), 임베딩 배치 제한, 목차 노이즈, "정보 없음" 메타 발언 저장 버그 등을 그날그날 발견·수정하는 사이클을 반복함.
- **보강 방향**: 이 사이클(문제 발견→원인 분석→수정→재검증) 자체를 진행 기록으로 계속 남겨두면, 일정 관리와 완성도 평가 양쪽에 다 쓸 수 있는 근거가 됨.

---

## 9. 시장조사 에이전트 recall 개선 아이디어 (2026-07-20)

Day 3 프로토타입 실행 결과, "정보 없음" 버그 수정 전후로 fact 수집량이 10개 → 4개로 줄어드는 현상을 관찰함(노이즈 제거와 회수율(recall) 사이의 트레이드오프). 향후 recall을 보완할 수 있는 방향을 정리한다.

- **`results_per_question` 값 증가**: 현재 질문당 검색결과 4건만 확인 중. 6~8건으로 늘리면 더 많은 스니펫에서 fact 추출 기회를 확보할 수 있음(다만 그만큼 임베딩·LLM 호출 비용 증가).
- **리서치 질문 재구성**: "신규 브랜드 평균 시장 진입 성공률"처럼 지나치게 구체적인 질문은 검색 스니펫에 답이 없을 확률이 높음. 질문 생성 프롬프트에 "검색 가능성(searchability)" 기준으로 한 번 더 필터링하는 단계를 추가하면 효율이 개선될 수 있음.
- **검색 결과 0건일 때 질문 재작성(query rewriting) 후 재검색**: 답을 못 찾은 질문은 더 일반적인 표현으로 자동 재검색하는 재시도 로직을 추가.
- **다중 검색 백엔드 활용**: `ddgs`는 메타서치 라이브러리라 DuckDuckGo 외에 다른 백엔드도 지원함. 백엔드를 바꿔가며 결과를 합치면 recall 향상 가능.
- **스니펫 대신 원문 페이지 본문 활용**: 지금은 검색 스니펫(1~2문장)만 보고 fact를 추출하는데, 스니펫이 짧아 정보가 부족한 경우가 많음. 상위 결과 몇 개는 실제 페이지 본문을 가져와서(예: `requests` + 본문 추출 라이브러리) 더 풍부한 텍스트에서 fact를 뽑으면 recall이 크게 개선될 가능성이 있음(비용·속도 트레이드오프 고려 필요).

**우선순위**: 위 recall 개선 작업은 경쟁사비교 에이전트 구현 이후 남은 작업 중 최우선 과제로 두기로 함(2026-07-20 사용자 결정). 이후 다른 신규 에이전트(검증·출처, Writer, Orchestrator)보다 먼저 착수할 것.

---

## 10. PESTEL 에이전트 구현 및 중복 fact 발견·수정 (Day 4, 2026-07-20)

### 10-1. PESTEL 에이전트 구현

- **코드 구조**: `agents/pestel.py`. `tag_facts()`가 Fact Store에 저장된 fact 전체를 한 번의 배치 LLM 호출로 6축(Political/Economic/Social/Technological/Environmental/Legal) 태깅 + 임팩트 방향(기회/위협/중립) + 시급성(즉시/단기/장기) 판정 후 `save_fact()`로 upsert. `summarize_by_axis()`가 축별로 fact를 묶어 내러티브 요약을 생성하며, 중요도 낮은 fact는 `excluded_low_materiality_fact_ids`에 투명하게 기록하고 삭제하지 않음. `run_pestel_analysis()`는 새로 웹검색을 하지 않고, `topic_relevance`에 topic 문자열이 포함된 기존 fact만 필터링해 재사용.
- **현재 상태**: 실행 결과 6축 태깅과 축별 요약 모두 정상 동작 확인. 다만 이 실행에서 대상 fact가 **26개** 로드되었는데, 이는 같은 topic으로 시장조사 에이전트를 여러 번 실행하며 쌓인 결과였고, 그 중 상당수가 표현만 다른(예: 어미 차이) 사실상 동일한 fact였음이 확인됨.
- **한계(당시)**: PESTEL 에이전트 자체는 이 중복을 걸러내는 로직이 없어, 같은 내용의 fact가 여러 번 태깅·요약에 포함되는 비효율(과대표집)이 발생할 수 있는 구조였음.

### 10-2. 중복 fact 발견 사례와 Fact Store 레벨 수정

- **발견 경위**: 위 10-1의 PESTEL 실행에서 26개 fact를 확인하는 과정에, "84억 4천만 달러이다" vs "84억 4천만 달러입니다"처럼 문장 어미·조사만 다르고 내용이 동일한 fact가 여러 건 중복 저장되어 있음을 실제로 관찰함. 원인은 시장조사 에이전트를 같은 topic으로 반복 실행할 때마다 매번 새 `fact_id` (`uuid4`)로 저장되어, 저장 시점에 기존 fact와 비교하는 절차가 전혀 없었기 때문.
- **수정 내역**: 표집 단계(시장조사 에이전트)가 아니라 **저장 단계(Fact Store)** 에 중복 방지 로직을 추가하는 방향으로 결정. `fact_store/store.py`에 `find_similar_fact(text, threshold=0.85)`(기존 fact 전체와 `difflib.SequenceMatcher` 문자열 유사도 비교) + `save_fact_if_new(fact)`(유사한 기존 fact가 있으면 저장하지 않고 기존 것을 반환, 없으면 신규 저장) 추가. `agents/market_research.py`의 fact 저장 루프를 `save_fact()` → `save_fact_if_new()`로 교체하고, 실행 끝에 `n_saved`(신규 저장 수)/`n_duplicates`(중복 건너뜀 수) 집계를 출력하도록 변경.
- **검증**: 임시 DB로 3건 테스트 — "84억 4천만 달러이다" vs "...입니다" 쌍은 중복으로 정확히 걸러짐(신규 아님), 완전히 다른 내용의 fact는 정상적으로 신규 저장됨을 확인. 사용자 환경(로컬 Mac)에서의 실제 재실행 검증은 아직 진행 중.
- **한계**: (1) 임베딩 기반 의미 유사도가 아니라 문자열 유사도(difflib)이므로, 완전히 다른 표현으로 같은 내용을 말하는 의역(paraphrase)까지는 잡아내지 못함. (2) `find_similar_fact()`가 매 저장 시도마다 `list_facts()`로 기존 fact 전체를 불러와 순회하는 O(n) 구조라, fact 수가 크게 늘어나면 저장이 느려질 수 있음(현재 규모에서는 문제없음). (3) 임계값 0.85는 실험적으로 튜닝한 값이 아니라 이번 관찰 사례("이다"/"입니다" 차이)에 맞춰 근사치로 설정한 값.
- **보강 방향**: (1) 검증·출처 에이전트 구현 시, 문자열 유사도 대신 임베딩(Solar Embeddings) 코사인 유사도 기반 중복 판정으로 고도화하면 의역까지 잡아낼 수 있음(8-2-1의 출처 검증 로직과 함께 묶어 구현 가능). (2) fact 수가 늘어나면 `list_facts()` 전체 순회 대신 같은 `topic_relevance`를 공유하는 fact로 비교 범위를 좁히는 최적화 고려. (3) 임계값은 실제 운영 데이터가 쌓이면 재조정 필요.

---

## 11. 경쟁사비교 에이전트 구현 및 경쟁사 식별 누락(recall) 발견·수정 (Day 5, 2026-07-20)

### 11-1. 경쟁사비교 에이전트 구현

- **코드 구조**: `agents/competitor.py`. (1) `generate_competitor_queries()` — 브랜드명·점유율·가격이 검색결과에 나올 확률이 높은 질문 생성. (2) `extract_competitor_facts()` — 기업명이 명시된 문장만 fact로 추출(시장조사 에이전트의 `extract_facts()`와 동일한 이중 안전장치인 `_is_no_info_statement()`를 그대로 재사용). (3) `identify_competitors()` — 저장된 fact 전체(topic으로 필터링)에서 실제로 언급된 경쟁사만 LLM이 식별해 직접/간접(대체재)/잠재 진입자로 분류. (4) `build_competitor_profiles()` — 경쟁사별로 "그 경쟁사가 언급된 fact만" 근거로 가격/핵심기능/타깃고객/채널/투자·매출/강점/약점을 구조화(근거 없는 필드는 지어내지 않고 빈 값). (5) `Competitor` 객체로 SQLite에 저장, 경쟁사(행)×비교축(열) 형태로 콘솔 출력.
- **설계 재사용**: client 생성 함수를 새로 만들지 않고 `agents.market_research.get_client()`를 그대로 가져다 씀 — 6-1절에서 지적한 "client 생성 함수가 파일마다 중복 정의된다"는 한계를 이 파일에서는 피함. fact 저장도 `save_fact_if_new()`를 그대로 써서 시장조사 에이전트가 이미 수집한 fact와 자동으로 중복 검사됨.

### 11-2. 경쟁사 식별 누락(recall 문제) 발견

- **발견 경위**: "웨어러블 헬스케어 기기 / 북미 시니어 건강관리 시장"으로 실행한 결과, fact 26개(이번에 새로 모은 7개 + 기존 19개) 중 "핏비트(FitBit)가 여러 밴드형 제품을 선보였다", "혈당 연동은 가민이 강점", "혈압·심전도는 갤럭시 워치가 강점", "혈압 특화는 화웨이가 강점"처럼 애플 외의 브랜드명이 fact 텍스트에 명확히 등장했음에도, `identify_competitors()`가 "Apple" 단 1개만 경쟁사 후보로 반환함. 시장조사 에이전트의 TAM/SAM/SOM 출처 조작 문제(8-2-1절)와는 반대 방향의 실패 패턴 — 이번엔 "없는 걸 지어낸" 게 아니라 "있는 걸 놓친" 경우.
- **추정 원인**: 배치 프롬프트가 "경쟁사를 찾아 분류하라"고만 지시했을 뿐, "fact 목록 전체를 끝까지 훑어 언급된 모든 기업을 빠짐없이 포함하라"는 완전성(completeness) 지시가 없었음. LLM이 가장 두드러지는 기업(Apple) 하나로 수렴해버린 것으로 추정(확정적 원인 분석은 아님 — 재현 실험은 아직 안 함).
- **수정 내역**: (1) `identify_competitors()` 프롬프트에 "가장 유명한 기업 하나만 고르지 말고, fact 목록 전체에서 서로 다른 기업/브랜드명이 언급될 때마다 빠짐없이 별도 항목으로 포함하라"는 완전성 지시를 구체적 예시(애플·핏빗·가민·삼성·화웨이 5개 예시)와 함께 추가. (2) `KNOWN_COMPETITOR_BRANDS` 브랜드명 목록 + `_flag_missed_competitor_mentions()` 함수로, fact 텍스트에 등장하는 알려진 브랜드명 중 후보 목록에 없는 것이 있으면 경고를 출력하는 코드 레벨 이중 안전장치 추가(시장조사 에이전트의 `KNOWN_RESEARCH_FIRMS`/`_flag_fabricated_citations()`와 반대 방향의 완전성 체크 — "지어낸 출처 탐지"가 아니라 "놓친 경쟁사 탐지").
- **검증**: 가상의 fact 3건(애플워치/핏비트/가민 언급)과 후보 1건(Apple)만 있는 상태로 `_flag_missed_competitor_mentions()`를 단위 테스트 — "Fitbit", "가민", "애플", "애플워치", "핏비트"가 모두 경고에 잡히는 것을 확인. 다만 이 테스트에서 "애플"/"애플워치"까지 경고에 잡힌 것은 사실 정상 식별된 "Apple"과 같은 회사인데 한글/영문 표기가 달라 문자열 매칭이 안 된 **거짓 양성(false positive)**임 — 아래 한계 참고. 프롬프트 수정 후 실제 재실행 검증은 사용자 로컬 환경에서 아직 진행 전.
- **한계**: (1) `KNOWN_COMPETITOR_BRANDS`는 웨어러블 헬스케어 시장에 흔한 브랜드만 하드코딩한 목록이라, 목록에 없는 새 브랜드는 여전히 놓칠 수 있음(시장조사 에이전트의 `KNOWN_RESEARCH_FIRMS`와 동일한 유형의 한계). (2) 경고 로직이 브랜드명 문자열의 부분 포함 여부만 보기 때문에, 같은 회사의 한글/영문 표기(애플 vs Apple)를 다르게 인식해 위 검증 사례처럼 이미 식별된 경쟁사를 "놓쳤다"고 잘못 경고하는 거짓 양성이 발생함. (3) 완전성 프롬프트 지시는 재발을 100% 보장하지 않음(정보 없음 메타 발언 문제처럼 프롬프트만으로는 불완전).
- **보강 방향**: (1) 거짓 양성을 줄이려면 브랜드명 동의어(애플=Apple, 삼성=Samsung=갤럭시)를 매핑하는 별도 사전을 두거나, 문자열 매칭 대신 임베딩 유사도로 "이미 식별된 후보와 같은 회사를 가리키는지" 판정하는 방식으로 고도화 가능(10절의 fact 중복 판정 고도화 방향과 같은 결). (2) `_flag_missed_competitor_mentions()`는 지금은 경고만 출력하고 자동으로 후보에 추가하지 않음 — 검증·출처 에이전트 구현 시, 경고된 브랜드를 후보 목록에 자동 추가하는 재시도 로직으로 확장할 수 있음. (3) 사용자 로컬 환경에서 프롬프트 수정 버전으로 재실행해, 실제로 핏빗·가민·삼성·화웨이가 별도 경쟁사로 식별되는지, `⚠️ 완전성 경고`가 (거짓 양성 없이) 정상 동작하는지 확인 필요.

### 11-3. 진짜 원인 발견: topic_relevance 문자열 필터가 fact를 통째로 누락시킴 (재실행 후 발견·수정)

- **발견 경위**: 11-2의 프롬프트 수정을 반영해 재실행했는데도 여전히 Apple 1개만 식별됨. 이번엔 로그를 자세히 보니, "삼성(갤럭시워치4...)", "레노버·샤오미...", "핏빗(Versa 3)..." 같은 fact가 분명히 이번 실행 중 새로 저장(`[fact 저장]`)됐는데도, 이후 "대상 fact"에는 포함되지 않았음. 또한 가격/채널/투자·매출이 모든 경쟁사에서 계속 "정보 없음"으로 나오는 것도 같은 원인이었음 — 가격·기능 비교를 다루는 fact 자체가 애초에 분석 대상에서 빠졌기 때문.
- **원인**: `run_competitor_analysis()`가 경쟁사 식별·프로필링 대상 fact를 고를 때 `[f for f in list_facts() if topic in (f.topic_relevance or "")]`처럼, fact의 `topic_relevance`(그 fact를 뽑아낸 검색 질문 문구 원문)에 `topic` 문자열이 그대로 포함되는지로 걸러냈음. 그런데 `generate_competitor_queries()`가 만든 질문은 매번 topic 문구를 그대로 쓰지 않고 재구성함 — 예: topic="웨어러블 헬스케어 기기"인데 질문은 "애플 워치, 갤럭시 워치, 핏빗 시니어 건강관리 기능별 가격 비교 2023"처럼 브랜드명 나열로 바뀜. 이 경우 substring 매칭이 실패해, 방금 막 저장한 fact조차 분석 대상에서 통째로 빠지는 구조였음. (이 substring 휴리스틱은 `agents/pestel.py`의 `run_pestel_analysis()`에도 동일하게 쓰이고 있고, 그 파일 주석에 이미 "여러 주제로 반복 실행 시 서로 섞이지 않도록"이라는 목적으로 명시돼 있었지만, 검색 질문이 topic 문구를 그대로 포함하지 않을 수 있다는 부작용까지는 그때 고려하지 못했음.)
- **수정 내역**: `run_competitor_analysis()`에서 이번 실행 중 실제로 다룬 fact(`run_facts` — 신규 저장분+중복으로 판정된 기존 fact 포함, 검색 루프에서 직접 참조를 들고 있으므로 topic_relevance 문구와 무관하게 항상 포함 가능)를 우선 전부 포함하도록 변경. 과거 fact(이전 세션의 시장조사 결과 등)는 기존처럼 `topic_relevance` substring 매칭으로 보조적으로 합침. 즉 "이번 실행 fact는 무조건 포함 + 과거 fact는 휴리스틱으로 보완"하는 2단 구조로 바꿈.
- **한계**: 과거 fact(직접 들고 있지 않아 substring 매칭에 의존하는 부분)는 여전히 같은 문제를 가짐 — 예를 들어 다른 세션에서 브랜드명 중심으로 재구성된 질문으로 저장된 과거 fact는 여전히 못 찾을 수 있음. `agents/pestel.py`의 `run_pestel_analysis()`는 이번 수정 대상에서 빠졌음 — PESTEL은 자체 검색을 하지 않고 항상 과거 fact만 재사용하므로 "이번 실행 fact"라는 개념이 없어 같은 방식으로 고칠 수 없고, substring 휴리스틱의 한계를 그대로 안고 있음.
- **보강 방향**: 근본적인 해결은 `Fact` 스키마에 검색 질문 문구와 별개로 "이 fact가 어떤 topic(연구대상)에 속하는지"를 명시하는 전용 필드(예: `topic: Optional[str]`)를 추가하고, 모든 조회를 이 필드의 정확한 일치로 하는 것. `topic_relevance`는 지금처럼 "어떤 질문에 답했는지" 설명용으로 남기고 분리. 이 변경은 검증·출처 에이전트 및 PESTEL 재사용 로직에도 함께 적용해야 함(현재는 competitor.py만 우선 완화). 스키마 필드 추가 자체는 SQLite에 `data_json`으로 저장되는 구조라 마이그레이션 없이 가능하나(기존 레코드는 `topic=None`으로 로드됨), 조회 로직을 topic 필드 기준으로 바꾸는 작업은 아직 하지 않음.

### 11-4. 근본 수정: Fact 스키마에 전용 `topic` 필드 추가 (재실행 후 확인됨)

- **재확인 경위**: 11-3의 완화 조치(이번 실행 fact 무조건 포함) 적용 후 재실행했더니, 이번엔 검색 결과 자체에서 fact가 0건 추출돼(`fact 0개 신규 저장`) "이번 실행 fact"가 비어 있었고, 결국 전부 "과거 fact"에 의존하게 됐음. 그런데 이전 실행에서 저장된 삼성·레노버·샤오미·핏빗 fact들은 그 실행에서만 `run_facts`로 즉시 포함됐을 뿐, `topic_relevance`(예: "애플 워치, 갤럭시 워치, 핏빗 시니어 건강관리 기능별 가격 비교 2023")에는 여전히 topic 문구가 없어, 다음 실행부터는 다시 "과거 fact" 취급되어 substring 매칭에서 계속 누락됨. 즉 11-3의 수정은 "당장 이번 실행"만 구제했을 뿐, 데이터베이스에 영구히 남는 근본 문제는 그대로였음 — 11-3에서 예상했던 한계가 실제로 재현된 것.
- **수정 내역**: 11-3의 "보강 방향"에서 제안했던 근본 해법을 바로 구현함.
  1. `fact_store/schema.py`의 `Fact`에 `topic: Optional[str]` 필드 추가 (검색 질문 문구가 아니라, 실행 시 입력된 topic 원문을 그대로 저장).
  2. `agents/market_research.py`, `agents/competitor.py`의 fact 생성 시 `topic=topic`을 명시적으로 채움.
  3. `agents/competitor.py`, `agents/pestel.py`의 조회 로직을 "`topic` 필드가 있으면 정확히 일치(`f.topic == topic`)로 판정, 없으면(레거시 fact) 기존 `topic_relevance` 부분 문자열 매칭으로 폴백"하는 구조로 변경.
  4. `fact_store/store.py`의 `save_fact_if_new()`에 점진적 백필 로직 추가 — 중복으로 판정된 기존 fact에 `topic`이 비어 있고 새로 들어온 fact에는 `topic`이 있으면, 기존 레코드에 그 값을 채워 업데이트함. 완전한 일괄 백필은 아니고, 같은 내용의 fact가 topic이 지정된 채로 다시 발견될 때만 점진적으로 보정되는 방식.
- **검증**: 임시 DB로 시나리오 재현 테스트 — (1) topic 필드 없이 저장된 레거시 fact, (2) topic 필드가 명시된 신규 fact, (3) 레거시 fact와 거의 동일한 텍스트인데 topic이 지정된 재중복 fact, 이렇게 3건으로 실행. 결과: 재중복 fact는 정상적으로 중복 판정되고(신규 저장 안 됨), 그 과정에서 레거시 fact에 `topic`이 백필됨을 확인. 이후 `topic` 필드 기준(레거시는 폴백) 필터링 결과, 레거시 fact와 신규 fact 둘 다 정상적으로 대상에 포함됨을 확인.
- **한계**: (1) 한 번도 재중복되지 않는(즉, 이후 실행에서 같은 내용으로 다시 검색되지 않는) 레거시 fact는 영원히 `topic=None`으로 남아 substring 폴백에 의존함 — 완전한 소급 해결은 아님. (2) 사용자 로컬 DB에 이미 쌓인 기존 레거시 fact들(이번 대화에서 여러 번 테스트하며 저장된 것들)은 이 수정만으로 즉시 다 고쳐지지 않음 — 개발/테스트 단계 데이터이므로, 필요하면 `fact_store.db`를 초기화하고 처음부터 다시 수집하는 것이 가장 확실한 정리 방법. (3) 사용자 환경에서의 실제 재실행 검증은 아직 진행 전(임시 DB 시나리오 테스트만 완료).
- **보강 방향**: 검증·출처 에이전트 구현 시 이 `topic` 필드를 표준 조회 키로 채택하고, `topic_relevance` 폴백 경로는 레거시 데이터가 자연 소멸(재수집)되면 제거 가능. 필요하면 한 번에 모든 레거시 fact의 `topic`을 채워 넣는 별도 백필 스크립트(예: `topic_relevance`에 특정 topic의 하위 문자열이 포함된 fact를 찾아 일괄 업데이트)를 작성할 수도 있으나, 지금은 실용적으로 보류.
- **재실행 검증(2026-07-20)**: `fact_store/fact_store.db`를 실제로 삭제(정확한 경로: `fact_store/fact_store.db`, 최초 시도 때 사용자가 프로젝트 루트의 `fact_store.db`를 지웠으나 실제 파일 위치가 달라 삭제되지 않았던 해프닝이 있었음)하고 클린 상태로 재실행. 결과: (1) 시장조사 에이전트가 TAM 245.9억 USD를 정상 추출하고 SAM/SOM 비율 추론에서 가짜 출처 없이 "(업계 일반적 추정치, 별도 출처 없음)"을 정직하게 표기함 — 8-2-1절 수정이 클린 데이터에서도 여전히 유효함을 재확인. (2) 경쟁사비교 에이전트가 이번엔 새로운 문제를 드러냄 — 아래 11-5절 참고.

---

## 11-5. 경쟁사 식별의 정밀도(precision) 문제: 비경쟁 기관(FDA)이 경쟁사로 오식별됨 (2026-07-20 발견·수정)

- **발견 경위**: 클린 DB로 재실행한 결과, "2025년 9월, 애플워치 시리즈 9·10·11 및 Ultra 2·3가 미국 FDA로부터 고혈압 감지 기능을 공식 승인받았습니다"라는 fact 하나에서, `identify_competitors()`가 "애플워치"뿐 아니라 "FDA"까지 별도의 경쟁사(간접 경쟁자(대체재))로 분류해 반환함. FDA는 미국 식품의약국(규제기관)이지 시장에서 제품·서비스로 경쟁하는 주체가 아님 — 명백한 오분류.
- **의미**: 11-2/11-3이 다룬 문제(recall — 있는 경쟁사를 놓침)와 정반대 방향의 실패 — 이번엔 precision 문제(경쟁사가 아닌 것을 경쟁사로 잘못 포함함). "완전성을 높이라"는 11-2의 프롬프트 수정이 오히려 이런 과다포함 위험을 키울 수 있다는 트레이드오프를 보여주는 사례이기도 함.
- **수정 내역**: (1) `identify_competitors()` 프롬프트에 "정부기관·규제기관·표준기구(FDA, 식약처, ISO 등)는 경쟁사로 포함하지 말 것 — fact에 규제기관이 언급되더라도 그건 다른 기업의 속성일 뿐"이라는 제외 지시 추가. (2) `NON_COMPETITOR_ENTITY_PATTERNS` 하드코딩 목록 + `_is_non_competitor_entity()` 함수로, LLM이 그래도 반환한 후보 중 목록에 매칭되는 이름을 코드 레벨에서 걸러내는 이중 안전장치 추가(시장조사 에이전트의 `NO_INFO_PATTERNS` 패턴과 동일한 접근).
- **검증**: `_is_non_competitor_entity("FDA")` → True, `_is_non_competitor_entity("Apple")` → False로 단위 테스트 확인. 프롬프트 수정 반영 후 실제 재실행 검증은 아직 진행 전.
- **한계**: `NON_COMPETITOR_ENTITY_PATTERNS`는 지금까지 관찰된 기관명(FDA 등)과 흔히 등장할 만한 기관명을 하드코딩한 목록이라, 목록에 없는 다른 정부기관·표준기구·업계 단체는 여전히 경쟁사로 잘못 분류될 수 있음. 또한 이 필터는 이름 문자열 매칭만 하므로, 이름에 우연히 목록 단어가 포함된 실제 기업(예: 가상의 사례지만 "ISO텍" 같은 이름)을 잘못 제외할 거짓 양성 가능성도 이론적으로 있음.
- **보강 방향**: 검증·출처 에이전트 구현 시, "이 이름이 실제 시장 참여 기업인지"를 판별하는 별도의 엔티티 타입 분류 단계(회사/브랜드 vs 기관/단체 vs 인물 등)를 두면 이 문제를 프롬프트·휴리스틱 조합보다 더 견고하게 해결할 수 있음. 지금은 시간 제약상 하드코딩 목록 + 프롬프트 지시의 2단 완화책으로 대응.

---

## 12. recall 개선 작업 착수 (2026-07-20)

경쟁사비교 에이전트의 식별 정확도가 검증된 뒤(11-4/11-5), 최우선 과제로 미뤄뒀던(memory: recall 개선 우선순위) 9절의 recall 개선 아이디어 중 착수 가능한 항목을 실제로 구현함. 계기는 클린 DB 재실행에서 경쟁사 프로필 대부분이 "가격/채널/투자·매출: 정보 없음"으로 나온 것 — 지어낸 게 아니라, 수집된 fact 자체가 "누가 시장에 있는지"만 나열할 뿐 개별 기업의 가격·기능·채널 같은 세부 비교 정보를 담고 있지 않았기 때문(build_competitor_profiles는 설계대로 정직하게 빈 값 처리한 것).

### 12-1. 코드 구조

- **`agents/web_search.py`**: `fetch_page_text(url, max_chars=4000)` 추가 — `requests`로 페이지를 가져와 `<script>`/`<style>` 태그와 나머지 HTML 태그를 정규식으로 제거해 본문 텍스트만 남김(타임아웃 8초, 실패 시 예외를 삼키고 `None` 반환). `search_web()`에 `fetch_full_text: bool = False` 파라미터를 추가해, 켜면 각 검색결과에 스니펫 대신(또는 실패 시 스니펫으로 폴백) 실제 페이지 본문을 `content` 필드로 채움.
- **`agents/market_research.py`**: `simplify_query(client, question)` 추가 — 어떤 질문에서 fact를 하나도 못 뽑았을 때, 더 일반적이고 검색 가능성 높은 검색어로 재구성(query rewriting). `run_market_research()`의 검색·추출·저장 루프를 `_search_extract_and_save()` 헬퍼로 분리해, 한 질문에서 뽑은 fact 수를 반환하도록 바꾸고, 0개면 `simplify_query()`로 재구성한 질문으로 1회 재검색. `search_web()` 호출에 `fetch_full_text=True` 적용. `results_per_question` 기본값 4 → 6.
- **`agents/competitor.py`**: 동일한 구조(`_search_extract_and_save()` 헬퍼 + 0건 시 재검색 + `fetch_full_text=True`)를 그대로 적용. `simplify_query`는 새로 만들지 않고 `agents.market_research`에서 재사용(6-1절 client 함수 재사용과 같은 원칙). `results_per_query` 기본값 4 → 6.

### 12-2. 검증

- `fetch_page_text()`: 로컬 HTTP 서버로 `<script>`/`<style>`이 섞인 HTML을 응답하게 만들어 실제 요청 → 본문만 정확히 추출되는 것을 확인("Garmin Venu 3 model has fall detection." 케이스로 태그·스크립트 제거 검증).
- `simplify_query()`, `_search_extract_and_save()`의 재시도 분기: 문법 검증(`ast.parse`)만 완료, 실제 LLM 호출을 포함한 end-to-end 검증은 사용자 로컬 환경에서 진행 필요(이 세션의 원격 샌드박스는 Upstage/DuckDuckGo API에 네트워크 접근이 막혀 있어 실제 호출 테스트 불가 — 6-2절 이전에도 동일하게 겪은 제약).

### 12-3. 한계

- `fetch_page_text()`는 readability류 라이브러리 없이 정규식으로 태그만 제거하는 간이 추출이라, 네비게이션·광고·쿠키 배너 텍스트가 본문에 섞여 들어올 수 있음. 페이지가 크면 `max_chars=4000`으로 잘라 뒷부분 내용을 놓칠 수 있음.
- 매 검색결과마다 페이지를 추가로 요청하므로(기존 스니펫만 쓰던 것 대비) 질문당 API/네트워크 호출이 늘어 실행 시간이 길어짐. `results_per_question`도 4→6으로 늘렸으니 체감 실행 시간이 눈에 띄게 길어질 수 있음.
- `simplify_query()`의 재시도는 질문당 최대 1회로 제한(무한 재시도 방지)했지만, 재시도해도 여전히 0건이면 그 질문은 그냥 넘어감 — 근본적으로 ddgs가 그 주제에 대한 결과 자체를 갖고 있지 않은 경우는 해결 못 함.
- 봇 차단(403 등)이 있는 사이트는 `fetch_page_text()`가 `None`을 반환해 스니펫으로 자동 폴백하지만, 이 경우 recall 개선 효과가 그 결과에 한해서는 없음.

### 12-4. 보강 방향

- 사용자 로컬 환경에서 재실행해 (1) 기존보다 fact 수·다양성이 늘었는지, (2) 경쟁사 프로필의 가격/기능/채널 필드가 실제로 채워지는지, (3) 실행 시간이 허용 범위인지 확인 필요.
- `max_chars`를 늘리거나 페이지를 섹션 단위로 나눠 여러 번 요약하는 방식도 고려 가능(비용 트레이드오프).
- 다중 검색 백엔드(ddgs가 지원하는 다른 백엔드) 활용은 아직 미착수 — 이번 페이지 본문 활용과 query rewriting 효과를 먼저 확인한 뒤 필요하면 추가.

---

## 12-5. recall 개선의 부작용: target_market·topic 무관 fact 대량 유입 (2026-07-20 발견·수정)

- **발견 경위**: 12절 recall 개선(페이지 본문 전체 활용 + 결과 수 증가 + query rewriting)을 반영해 클린 재실행한 결과, fact 수 자체는 크게 늘었음(시장조사 41개, 경쟁사비교 63개, 경쟁사 후보 36개). 그런데 그 중 상당수가 topic("웨어러블 헬스케어 기기")·target_market("북미 시니어 건강관리 시장")과 실제로 무관했음. 관찰된 사례: "SK건설이 인천 아파트 단지에 웨어러블 기기 적용"(한국 건설사·아파트 부대시설, 북미 아님), "짧은 말초정맥 카테터(Short PIVCs) 시장"(카테터는 웨어러블 헬스케어 기기가 아님), "덱스컴 G7은 **한국**에서 유일하게 식약처 인증"(한국 얘기), "갤럭시워치는 **한국소비자원**에서 비교한 결과..."(역시 한국). 이 오염된 fact들이 그대로 `identify_competitors()`에 넘어가면서, SK건설·SK텔레콤(한국 건설/통신사)과 BD·B.Braun·Terumo·Teleflex·Smiths Medical(카테터/IV 의료기기 회사, 완전히 다른 시장)까지 "경쟁사"로 잘못 식별됨.
- **원인**: 12절에서 스니펫 대신 페이지 본문 전체를 fact 추출 재료로 쓰기 시작했는데, `extract_facts()`/`extract_competitor_facts()`의 프롬프트는 "리서치 질문에 답이 되는 사실"인지만 체크했을 뿐, 그 사실이 실제로 topic(제품 카테고리)·target_market(지역)에 부합하는지는 체크하지 않았음. 페이지 본문 전체에는 검색 질문과 우연히 겹치는 키워드(예: "웨어러블", "혈압", "시니어")를 포함한 무관한 단락이 섞여 있을 수 있는데, 이걸 추출 단계가 걸러내지 못한 것. recall을 올리려고 자료 폭을 넓힌 대가로 정밀도(precision)가 떨어진, 전형적인 트레이드오프 사례.
- **수정 내역**: `extract_facts()`(시장조사 에이전트), `extract_competitor_facts()`(경쟁사비교 에이전트) 프롬프트에 `topic`·`target_market`을 명시적으로 전달하고, "목표시장과 다른 국가/지역 얘기는 제외", "연구대상과 다른 제품 카테고리 회사 얘기는 제외"라는 관련성 필터 지시를 추가함. 이에 맞춰 두 함수의 시그니처에 `topic: str, target_market: str` 파라미터를 추가하고, `_search_extract_and_save()` 헬퍼와 `run_market_research()`/`run_competitor_analysis()`까지 이 값을 그대로 전달하도록 호출 체인을 수정함.
- **검증**: 문법 검증(`ast.parse`)만 완료. 프롬프트 지시 강화이므로 실제 효과는 LLM 호출이 필요해, 사용자 로컬 환경에서 재실행 검증이 필요함.
- **한계**: (1) 시장조사 에이전트의 `_is_no_info_statement()`나 경쟁사비교 에이전트의 `NON_COMPETITOR_ENTITY_PATTERNS`처럼, 이번 관련성 필터는 코드 레벨 이중 안전장치가 아직 없음(프롬프트 지시 단일 계층) — LLM이 지시를 놓치면 여전히 무관한 fact가 섞일 수 있음. (2) "지역 무관"·"카테고리 무관" 판정은 FDA 같은 고유명사 목록화가 쉬운 문제가 아니라(예: "한국"이라는 단어가 항상 무관하다고 단정할 수 없음 — 목표시장이 한국이면 반대로 관련 있음), 간단한 키워드 하드코딩 이중 안전장치를 만들기 어려움. 지금은 프롬프트 지시에만 의존.
- **보강 방향**: (1) 재실행 후에도 무관한 fact가 계속 섞이면, "이 fact가 target_market/topic에 관련 있는가"를 별도 LLM 호출로 한 번 더 검증하는 2단계 필터(추출 → 관련성 재검증)를 추가하는 방안 고려. (2) 검증·출처 에이전트 구현 시, fact 저장 이후에도 "이 fact가 애초에 지정된 topic·target_market과 일치하는가"를 재검사하는 사후 감사(post-hoc audit) 단계를 둘 수 있음. (3) 지금까지 클린 DB에 이미 쌓인 오염된 fact들은 이 프롬프트 수정만으로 소급 정리되지 않으므로, 다시 한번 `fact_store/fact_store.db`를 초기화하고 재실행해 확인하는 것을 권장.

---

## 13. 검증·출처(Reviewer) 에이전트 — 논문 검토 및 설계 (2026-07-21)

11~12절에서 반복적으로 겪은 recall↔precision 시소(경쟁사 식별 recall 문제 → precision 문제 → recall 개선 → precision 재발)를 매번 프롬프트 땜질로 대응하는 대신, RAG 분야에서 "검색 결과의 노이즈를 어떻게 걸러내는가"를 정면으로 다룬 기존 연구를 검토하고, 그 결과를 검증·출처 에이전트 설계에 반영했다.

### 13-1. 검토한 논문 요약

- **CRAG (Corrective RAG, arXiv:2401.15884)**: 검색된 문서를 경량 T5-large 평가모델로 Correct/Incorrect/Ambiguous 3단계 채점. Ambiguous·Incorrect일 때 웹 검색으로 보강. 핵심 아이디어는 decompose-then-recompose — 문서를 문단 단위 조각(strip)으로 쪼개 조각별로 관련성을 채점하고, 관련 있는 조각만 다시 이어붙여 최종 근거로 사용.
- **DataCamp CRAG/LangGraph 튜토리얼**: CRAG를 실전 구현한 예시로, 전용 소형 모델 대신 GPT-4o-mini + 프롬프트(관련 있음/없음 이진 분류)로 문서를 채점. `grade_documents` 노드가 채점하고, 관련 문서 비율이 70% 이하면 질의를 재구성(`transform_query`)해 웹 검색을 재시도하는 조건부 엣지(`decide_to_generate`) 구조.
- **Self-RAG (arXiv:2310.11511)**: 검색 여부·문서 관련성·생성 근거지지도·유용성을 LLM 자신이 "reflection token"으로 함께 생성하도록 엔드투엔드 학습. 별도 채점 모델을 추론 시점에 띄울 필요가 없다는 게 장점이나, 이를 위해 전용 모델을 파인튜닝해야 하는 무거운 접근.
- **MAIN-RAG (arXiv:2501.00332)**: training-free. 여러 LLM 에이전트가 독립적으로 문서를 채점한 뒤, (1) 점수 분포 기반 적응형 임계값과 (2) 에이전트 간 합의(consensus)로 관련 문서를 걸러낸다. 파인튜닝·추가 학습 데이터 없이 4개 QA 벤치마크에서 정확도 2~11%p 개선.
- **Tackling the Inherent Difficulty of Noise Filtering in RAG (arXiv:2601.01896)**: 노이즈 필터링이 제한된 트랜스포머 레이어·어텐션 구조상 근본적으로 어려운 문제이며, 표준적인 파인튜닝으로도 잘 해결되지 않는다고 주장. 이를 겨냥한 별도 파인튜닝 기법을 제안하나, 본문 상세는 확인하지 못함(초록만 확인).

### 13-2. 왜 이 조합을 선택했는가

Self-RAG와 2601.01896은 둘 다 전용 모델 파인튜닝이 필요하다. Upstage 콘솔(`console.upstage.ai/docs`)을 직접 확인한 결과 Solar 계열은 자체 파인튜닝 엔드포인트를 제공하지 않고(과거 Solar 파인튜닝은 서드파티 Predibase 경유), 2주 MVP 일정에도 맞지 않아 제외했다. 반면 CRAG의 decompose-then-recompose와 MAIN-RAG의 training-free 다중 에이전트 합의는 지금 쓰고 있는 Solar Mini + 프롬프트 방식 그대로 얹을 수 있어 채택했다.

### 13-3. 설계 — 파이프라인 상 위치

`_search_extract_and_save()`(market_research.py, competitor.py 공통 구조) 안에서, 검색 결과를 fact로 추출하기 **직전**과 fact를 Fact Store에 저장한 **직후** 두 지점에 검증 로직을 끼워 넣었다.

- 저장 전(추출 전) — 청크 필터링: 무관한 본문이 애초에 fact 추출 프롬프트에 보이지 않게 한다.
- 저장 후 — fact 채점: 이미 구조화된 fact 문장 단위로 관련성·근거지지도를 재검증한다.

이렇게 두 지점을 둔 이유는, 청크 필터링만으로는 "본문은 관련 있지만 LLM이 그 내용을 과장/왜곡해서 fact로 뽑아내는" 경우(근거지지도 문제)를 못 잡고, fact 채점만으로는 "애초에 무관한 내용이 추출 프롬프트에 다량 노출돼 추출 자체가 오염되는" 문제(현재 12-5절에서 겪은 문제)를 못 잡기 때문이다.

### 13-4. 설계 — 청크 분해(CRAG decompose-then-recompose 적용)

`agents/web_search.py`에 `chunk_text(text, chunk_size=500, overlap=50)`를 추가해 본문을 슬라이딩 청크로 분해한다. `fetch_page_text()`가 정규식으로 태그를 제거하면서 모든 공백을 단일 스페이스로 뭉개 문단 경계 정보가 사라지므로, 문단 단위 대신 고정 길이+겹침(overlap) 방식을 택했다 — fact 하나가 청크 경계에 걸쳐 있어도 최소 한쪽 청크는 온전히 포함하도록 하기 위함이다.

`agents/verification.py`의 `filter_relevant_chunks()`가 이 청크들을 배치로(청크마다 개별 호출이 아니라 한 번의 LLM 호출로) 관련성 채점하고, 관련 있다고 판정된 청크만 다시 이어붙여 `extract_facts()`/`extract_competitor_facts()`에 넘긴다. 본문이 짧아 청크가 1개면 이 단계를 건너뛰어(LLM 호출 없이 그대로 반환) 비용을 아낀다.

### 13-5. 설계 — fact 단위 검증(관련성 + 근거지지도, MAIN-RAG식 합의)

`fact_store/schema.py`에 `VerificationStatus`(채택/애매/기각) enum과 `verification_status`, `verification_reasoning` 필드를 추가했다(기존에 있던 `citation_verified`/`verification_score`/`needs_source_check`는 하위 호환을 위해 그대로 두고, 최종 판정과 연동되도록 값을 채운다).

`agents/verification.py`의 `grade_fact()`가 같은 Solar Mini 모델에 관점이 다른 두 프롬프트(관련성 전용/근거지지도 전용)를 줘서 독립적인 두 점수(1~5점)를 얻고, 보수적으로 결합(min)한다. 완전한 다중 모델 다중 에이전트(MAIN-RAG 원 논문 방식)는 비용·일정상 무리라 판단해, "같은 모델·다른 관점 프롬프트 2회"로 축소했다.

임계값은 고정값이 아니라 `compute_adaptive_threshold()`가 그 배치(한 번의 검색 질문에서 새로 저장된 fact들)의 점수 분포(평균-0.5×표준편차, 3~4점 범위로 clamp)를 보고 정한다(MAIN-RAG의 adaptive filtering 적용). 결합 점수가 임계값 이상이면 채택, 임계값-2 이하면 기각, 그 사이면 애매로 분류한다.

### 13-6. 판정 결과의 다운스트림 처리

CRAG의 3단계 판정과 PESTEL 에이전트의 Materiality Filter(10절) 원칙을 결합해, "기각"된 fact는 Fact Store에서 삭제하지 않되 PESTEL(`run_pestel_analysis`)·경쟁사비교(`run_competitor_analysis`)의 분석 대상 fact 목록에서는 제외한다. "애매" 판정은 현재 버전에서는 분석 대상에 남겨두되(과도한 recall 손실 방지), `needs_source_check=True`로 표시해 사람이 재확인할 수 있게 한다 — CRAG가 제안한 "애매 시 재검색" 재시도 로직은 이번 구현에는 포함하지 않았다(비용·일정 트레이드오프로 다음 보강 대상).

### 13-7. 한계 및 보강 방향

- 관점 2개를 "같은 모델·다른 프롬프트"로 구현했기 때문에, MAIN-RAG가 말하는 완전히 독립적인 다중 에이전트 합의보다는 오류 상쇄 효과가 약할 수 있다. 예산이 허락하면 Solar Pro2를 세 번째 채점자로 추가하는 것을 고려할 수 있다.
- "애매" 판정에 대한 CRAG식 재검색 재시도(질의 재구성 후 1회 더 검증)는 아직 구현하지 않았다.
- 청크 분해가 고정 길이 방식이라 문장이 청크 경계에서 잘릴 수 있다(overlap=50자로 완화하나 완전히 해결되지는 않음).
- 실제 채점 정확도(이 LLM 채점 자체가 얼마나 신뢰할 만한가)는 사용자 로컬 환경에서 재실행하며 실측 검증이 필요함 — 지금은 문법 검증(`py_compile`, import 확인)까지만 완료.

---

## 13-8. 첫 실행에서 발견된 문제: 근거지지도 오탐(false rejection) + 진단 정보 소실 (2026-07-21 발견·수정)

- **발견 경위**: 검증 에이전트 구현 후 첫 실제 실행(시장조사 에이전트)에서, 원화(KRW) 가격 정보처럼 명백히 걸러내야 할 fact는 정확히 기각됐으나("가격대는... 10만 원대부터... 40만~60만 원대"), 동시에 누가 봐도 topic·target_market에 정확히 부합하는 fact도 1점으로 오기각됐다. 예: "애플워치의 낙상 감지 기능은 심한 낙상 시 응급 서비스 및 긴급 연락처에 자동 알림 기능을 제공함", "북미 시니어 건강관리 시장에서... 낙상 감지 기능은 실제 만족도가 높은 것으로 나타남".
- **원인 ①(진단 정보 소실, 코드 버그)**: `verify_facts_batch()`가 관점별 채점 이유(reasoning)를 받아놓고도 결합 점수만 남기고 버려서, 왜 저 두 fact가 기각됐는지(관련성 문제인지 근거지지도 문제인지) 로그에 전혀 남지 않았음. `verify_fact()`(단일 fact용 함수)는 애초에 reasoning을 잘 보존하는데, 배치 버전을 별도로 만들면서 그 부분을 누락한 것 — 같은 함수를 재사용하지 않고 배치 최적화를 위해 로직을 중복 작성하다 생긴 실수.
- **원인 ②(추정, 프롬프트 설계 문제)**: 근거지지도(groundedness) 채점 프롬프트가 "원문에 명확한 근거 문장이 있는가"만 물어서, `extract_facts()`가 원래 하는 일(원문을 "사실 하나당 한 줄"로 **요약·재구성**하는 것)을 LLM이 "원문과 표현이 다르면 지어낸 것"으로 과도하게 엄격히 해석했을 가능성이 있음. fact는 원문의 의역이 정상인데, 근거지지도 프롬프트가 이 전제를 명시하지 않았던 것.
- **수정 내역**: (1) `verify_facts_batch()`에서 관점별 reasoning을 combined 점수와 함께 끝까지 보존해 `fact.verification_reasoning`에 저장하고, 기각/애매 판정 시 콘솔에 사유까지 출력하도록 수정(`print(f"    사유: {reasoning}")`). (2) 근거지지도 프롬프트에 "이 fact 문장은 원문을 요약·재구성한 것이며 표현이 다른 것은 감점 사유가 아니다, 원문에 없는 새로운 사실·수치가 추가된 경우에만 낮은 점수를 달라"는 지시를 명시적으로 추가하고, "애매하면 관대하게(3점 이상) 채점하라"는 지침도 덧붙임.
- **검증**: 문법 검증(`py_compile`, import)만 완료. 프롬프트 수정 효과는 실제 LLM 호출이 필요해 사용자 재실행 검증이 필요함.
- **한계**: 이번 수정으로도 오탐이 계속되면, 원인이 프롬프트가 아니라 Solar Mini 자체의 판단력 한계일 가능성도 배제할 수 없음 — 이 경우 관점별 reasoning을 실제로 로그에서 읽어보고, 관련성/근거지지도 중 어느 쪽이 계속 낮게 나오는지에 따라 (a) 해당 관점 프롬프트를 추가로 손보거나 (b) 채점 모델을 Solar Pro2로 상향하는 방안을 검토해야 함.
- **보강 방향**: 재실행 로그에서 `사유:` 줄을 반드시 확인해, 이번에 오기각된 fact들이 이제 통과하는지 + 만약 여전히 기각된다면 어느 관점(관련성/근거지지도) 점수가 낮게 나오는지 확인 필요.

---

## 13-9. DB 초기화 후 재실행 검증 결과 (2026-07-21)

- **결과**: 13-8절 수정(reasoning 보존 + 근거지지도 프롬프트 완화)이 실제로 효과가 있었음을 확인. 이번엔 명백한 조작 사례 2건을 정확히 근거지지도 1점으로 잡아냄(예: "위딩스 스캔워치2는... 독일 및 프랑스 수면 실험실의 AI 시스템..." → "원문에 구체적인 정보가 없어 지어낸 내용으로 보임", "주요 업체로는 Medtronic, Siemens, Fitbit, Panasonic..." → "완전히 지어낸 것으로 보임"). 이전처럼 명백히 관련 있는 fact가 오기각되는 사례는 관찰되지 않음.
- **새로 관찰된 패턴**: "채택"(결합점수 5점 근처) 판정이 거의 없고 대부분 "애매"(2~3점)에 몰림. 원인은 `extract_facts()`가 원래 하는 일(원문을 "사실 하나당 한 줄"로 요약·재구성)이 근거지지도 채점 시 거의 항상 "원문에 없는 세부사항이 약간 추가됨"으로 읽혀 3점으로 캡핑되기 때문. 결합점수가 min(관련성, 근거지지도)이라 관련성이 5점이어도 최종 3점(애매)이 됨.
- **영향 평가**: 애매 판정은 분석 대상에서 제외되지 않고(`needs_source_check=True`만 표시) 그대로 PESTEL·경쟁사비교 근거로 쓰이므로, 지금 당장 recall 손실이나 기능 장애로 이어지지는 않음. 다만 "채택/애매/기각" 3단계 구분이 사실상 "애매/기각" 2단계에 가깝게 좁혀져, CRAG가 원래 의도한 confidence 단계 구분의 효용이 약해진 상태.
- **보강 방향(우선순위 낮음, 차후 고려)**: 근거지지도 채점 기준을 "관련성처럼 5점 척도 전체를 쓰되, 사소한 재구성은 5점, 검증 불가능한 새 수치·주장 추가만 3점 이하"로 더 명확히 구분하거나, combined 계산을 min 대신 가중합(예: 관련성 0.6 + 근거지지도 0.4)으로 바꿔 근거지지도의 과도한 캡핑 효과를 완화하는 방안을 검토할 수 있음.

---

## 14. 경쟁사비교 프로필 3단계(심층조사) 추가 — 가격·채널·투자매출 공란 문제 (2026-07-21)

- **발견 경위**: 경쟁사비교 실행 결과를 보면 모든 경쟁사 프로필에서 공통적으로 "가격", "채널", "투자/매출" 항목이 거의 항상 "(정보 없음)"으로 비어있었다.
- **원인**: 사람이 실제로 경쟁사 분석을 할 때는 (1) 시장조사 → (2) 경쟁사 후보 확정 → (3) 경쟁사 개별 조사(홈페이지·SNS·증시 확인) → (4) 프로필 작성 순서를 밟는데, 기존 코드는 (2) 식별 직후 곧바로 (4) 프로필 구조화로 넘어가고 (3)이 아예 없었다. `build_competitor_profiles()`는 새로 검색하지 않고 (1) 단계에서 이미 모은 fact 중 "그 경쟁사가 언급된 것"만 재사용하는데, (1)단계 검색 질문 자체가 "시장 점유율 1~3위 기업", "브랜드별 가격대 비교"처럼 시장 전체를 훑는 질문이라 특정 회사를 정조준해서 가격·채널·투자매출까지 캐묻지는 않는다. 그래서 시장 전체 통계에 우연히 걸린 정보만 채워지고, 회사별 세부 정보는 구조적으로 계속 비어있었던 것.
- **수정 내역**: `agents/competitor.py`에 `deep_dive_competitors()`를 추가해 (3)단계를 구현. 경쟁사 식별(`identify_competitors`) 직후, 프로필 구조화(`build_competitor_profiles`) 이전에 실행된다.
  - 대상 범위 제한(비용 통제): 식별된 후보 중 "직접 경쟁자"만 대상으로 하고(간접 경쟁자·잠재 진입자는 제외), `mentioned_fact_ids` 개수(언급 빈도 = 시장 존재감 대리지표)가 많은 순으로 상위 `DEEP_DIVE_TOP_N`(기본 5)개만 고른다. 후보가 20개 넘게 나와도(실제 관찰: 24개) 전부 심층조사하지 않는다.
  - 질의 생성: `generate_competitor_queries()`처럼 LLM으로 질문을 새로 만들지 않고, 회사명 기반 고정 템플릿(`{name} 가격 공식 홈페이지 제품`, `{name} 투자 유치 매출 실적 규모`, `{name} 판매 채널 유통 파트너십`) 3개를 그대로 쓴다 — 회사명이 이미 확정된 뒤의 질문 생성은 창의적 작업이 아니라고 판단해 LLM 호출 비용을 아꼈다.
  - 검색·추출·검증은 기존 `_search_extract_and_save()`(청크 필터링 → fact 추출 → 저장 → 배치 검증)를 그대로 재사용 — 새 파이프라인을 만들지 않고 기존 것을 질의만 바꿔 재사용.
  - 새로 찾은 fact 중 실제로 회사명이 텍스트에 등장하는 것만 그 경쟁사의 `mentioned_fact_ids`에 추가(이중 안전장치 — 검색 질의에 이름을 넣어도 결과에 다른 회사 얘기가 섞여 나올 수 있어 한 번 더 확인).
  - `run_competitor_analysis()`에 `deep_dive_top_n` 파라미터를 추가(기본값 `DEEP_DIVE_TOP_N`), 0으로 주면 이 단계를 건너뛰고 기존 동작과 동일하게 작동.
- **검증**: 문법 검증(`py_compile`, import)만 완료. 실제 가격·채널·투자매출 공란이 채워지는지는 사용자 로컬 재실행 검증 필요.
- **한계**: (1) 상장사(Apple·Samsung·Google)는 이 단계로 개선 여지가 크지만, 비상장 소규모 업체(예: 에실로룩소티카, 케어웨어)는 애초에 투자·매출 정보가 공개되지 않은 경우가 많아 심층조사를 해도 "정보 없음"이 정답일 수 있다 — "안 찾아봐서 없음"과 "찾아봤는데 공개된 정보가 없음"을 구분하지 못하는 한계가 있음. (2) 홈페이지·SNS를 직접 방문하는 게 아니라 여전히 키워드 검색(ddgs)에 의존 — 공식 도메인을 알아내 그 안에서만 검색하는 site-restricted 검색은 아직 구현하지 않음. (3) 간접 경쟁자·잠재 진입자는 심층조사 대상에서 제외되므로, 이들의 가격·채널 정보는 여전히 공란일 가능성이 높음(의도된 비용 절충).
- **보강 방향**: 재실행 후 공란 비율이 얼마나 줄었는지 확인하고, 필요하면 (1) 상장사에 한해 SEC/DART 같은 2차 자료 도메인을 우선 검색하는 로직 추가, (2) 간접 경쟁자도 top_n을 늘려 포함하는 방안, (3) 여전히 공란인 항목은 "공개 정보 없음(확인 시도함)"과 "미확인"을 구분해 표시하는 방안을 검토.

---

## 15. 대부분의 fact가 "3차 자료"로 분류되는 문제 (2026-07-21 발견·수정)

- **발견 경위**: 사용자가 실행 로그를 보면서 "[fact 저장] (3차)"만 계속 나오고 2차 자료는 거의 안 보인다는 점을 지적함.
- **원인**: `search_web()`은 도메인 제한 없이 일반 웹검색(ddgs)을 수행하고, 각 결과 URL을 `classify_source_tier()`가 사후에 도메인 매칭으로 분류하는 구조다. 즉 2차 자료 검색 자체가 막혀있던 게 아니라, 분류 기준(`SECONDARY_DOMAIN_PATTERNS`)이 통계청·KOTRA·한국은행 등 **국내기관 위주**로 짜여 있고 해외 쪽은 Statista·Gartner·McKinsey·BCG·IBISWorld·SEC 6개뿐이었던 것이 원인. 지금 이 프로젝트가 다루는 topic·target_market은 대부분 북미 시장이라, 검색 결과에 실제로 자주 등장하는 Precedence Research·Grand View Research·MarketsandMarkets 같은 리서치 회사 도메인이 목록에 없어 전부 기본값(3차)으로 떨어졌다.
- **수정 내역**: `agents/web_search.py`의 `SECONDARY_DOMAIN_PATTERNS`에 북미/글로벌 시장조사 전문회사 도메인(precedenceresearch.com, grandviewresearch.com, marketsandmarkets.com, fortunebusinessinsights.com, alliedmarketresearch.com, researchandmarkets.com, marketresearchfuture.com, mordorintelligence.com, futuremarketinsights.com, transparencymarketresearch.com, coherentmarketinsights.com, emergenresearch.com, straitsresearch.com), 보도자료 배포 채널(globenewswire.com, prnewswire.com, businesswire.com), 미국 정부기관(cdc.gov, fda.gov, census.gov, cms.gov, 그 외 `.gov` 전체)을 추가함. 검색 로직 자체는 건드리지 않고 분류 목록만 확장.
- **검증**: `classify_source_tier()`에 대해 추가한 도메인들이 실제로 "2차"로 분류되는지 직접 호출해 확인 완료(예: precedenceresearch.com, grandviewresearch.com, marketsandmarkets.com, cdc.gov → 2차 / 일반 블로그 도메인 → 3차 그대로 유지).
- **한계**: 여전히 하드코딩된 도메인 목록 방식이라, 목록에 없는 새로운 리서치 회사·정부기관 도메인은 계속 3차로 분류된다. 완벽한 목록일 수 없다는 한계는 이전 절들의 다른 휴리스틱(NON_COMPETITOR_ENTITY_PATTERNS 등)과 동일.
- **보강 방향**: 재실행 후 2차 자료 비율이 실제로 늘었는지 로그로 확인. 장기적으로는 도메인 화이트리스트 대신, 알려진 리서치 회사명이 검색결과 title/URL에 등장하는지를 LLM이 판단하는 방식으로 일반화하는 방안도 고려할 수 있음(단, MVP 일정상 지금은 목록 확장으로 충분하다고 판단).

---

## 16. 심층조사(14절) 첫 실행 결과 진단 — 회사명/제품명 혼동 문제 (2026-07-21)

- **발견 경위**: 15절 도메인 목록 확장 후 시장조사·경쟁사비교를 재실행한 결과, 2차 자료가 실제로 나오기 시작한 것은 확인됐으나(예: "애보트 주식회사 링고 시스템은...(2차)"), 14절에서 구현한 심층조사 단계의 효과가 기대에 못 미침. 심층조사 대상 5개 중 삼성 갤럭시 워치·Fitbit·Omron Corporation 3곳은 3개 질의(가격/투자매출/채널) 모두 검색결과는 받았지만 fact를 하나도 저장하지 못함. 가격 항목은 5곳 전부 끝내 공란으로 남음. 애플·Abbott은 매출 관련 fact를 찾았는데도(`FY2025 기준 $109.2B...`, `Freestyle Libre 매출 $16억...`) 정작 프로필의 "투자/매출" 항목은 두 곳 다 "(정보 없음)"으로 나옴.
- **원인 진단**: 세 가지 원인 후보 중 사용자와 논의 후 "이름 정규화" 문제를 1순위로 확정.
  1. **회사명/제품명 혼동(확정, 이번에 수정)**: `identify_competitors()`가 후보명을 회사명이 아니라 제품/브랜드명으로 뽑는 경우가 있음(예: "삼성 갤럭시 워치"). 재무 정보(투자·매출)는 보통 제품 단위가 아니라 회사 단위로만 공시되므로, "삼성 갤럭시 워치 투자 유치 매출 실적"처럼 제품명 그대로 질의하면 애초에 나올 결과가 없음.
  2. **가격 질의 전멸(보류, 원인 미확정)**: 가격 전용 질의도 5곳 모두 0건이었던 것은 원인 후보 목록에 있었으나 이번엔 손대지 않음 — 청크 필터링/관련성 필터가 좁은 타겟 질의에 비해 너무 엄격할 가능성이 있음(다음 우선순위).
  3. **복합 문장에서 수치 추출 실패(보류, 원인 미확정)**: fact는 찾았는데 프로필 프롬프트가 보수적으로 빈칸을 남기는 문제도 이번엔 손대지 않음(다음 우선순위).
- **수정 내역(원인 ① 대응)**: `agents/competitor.py`에 `PRODUCT_TO_COMPANY_MAP`(제품명 → 실제 회사명 매핑, 예: "삼성 갤럭시 워치"→"삼성전자", "애플 워치"→"Apple", "Redmi"→"Xiaomi")과 `_company_name_for_deep_dive()`를 추가. `deep_dive_competitors()`가 질의를 만들 때만 이 정규화된 이름을 쓰고, 프로필에 표시되는 경쟁사명 자체(식별 단계의 원래 표기)는 바꾸지 않음. fact 귀속(attribution) 체크도 후보명과 정규화된 회사명 둘 중 하나라도 텍스트에 등장하면 인정하도록 확장(예: "삼성전자"라고만 언급된 fact도 "삼성 갤럭시 워치" 후보에 귀속 가능).
- **검증**: `_company_name_for_deep_dive()` 직접 호출로 매핑 동작 확인(`삼성 갤럭시 워치`→`삼성전자`, `애플 워치`→`Apple`, `Redmi`→`Xiaomi`, 목록에 없는 이름은 원본 그대로 유지). `py_compile`/import 확인 완료. 실제 검색 결과 개선 여부는 사용자 재실행 필요.
- **한계**: 하드코딩된 매핑이라 목록에 없는 새로운 제품명(예: 아직 안 나온 브랜드의 신제품)은 여전히 원본 그대로 검색된다 — 다른 휴리스틱 목록들과 동일한 한계.
- **보강 방향**: 재실행 후 삼성전자·Xiaomi 등으로 정규화된 대상의 매출/채널 공란이 줄었는지 확인. 여전히 남아있는 원인 ②(가격 질의 전멸)·③(복합 문장 수치 추출 실패)는 이번 재실행 결과를 보고 우선순위를 다시 정할 예정.

---

## 17. query rewriting 재검색이 결과 0건일 때 파이프라인 전체가 죽는 크래시 (2026-07-21 발견·수정)

- **발견 경위**: 16절 수정 재검증을 위해 재실행했는데, 시장조사 에이전트가 2번째 리서치 질문에서 fact를 못 찾아 `simplify_query()`로 재구성한 뒤 재검색하는 도중 처리되지 않은 예외로 프로그램 전체가 중단됨(`ddgs.exceptions.DDGSException: No results found.`). 이 때문에 남은 질문 2개, TAM/SAM/SOM 계산이 전혀 실행되지 못하고 시장조사 에이전트가 조기 종료됨.
- **원인**: `agents/web_search.py`의 `search_web()`이 `ddgs.text()` 호출을 아무 예외 처리 없이 그대로 호출하고 있었음. ddgs 라이브러리는 검색 결과가 정말 하나도 없을 때 빈 리스트를 반환하는 대신 `DDGSException("No results found.")`을 던지는 경우가 있는데, 이 경우의 수를 대비하지 않아 재시도(query rewriting) 질의처럼 결과가 안 나올 가능성이 있는 호출에서 그대로 크래시로 이어짐. `fetch_page_text()`는 이미 "네트워크 오류 시 None 반환"으로 실패를 흡수하도록 만들어져 있었는데, `search_web()`에는 같은 안전장치가 없었던 것.
- **수정 내역**: `search_web()` 전체를 try/except로 감싸, 예외 발생 시 경고 메시지를 출력하고 빈 리스트(`[]`)를 반환하도록 수정. `fetch_page_text()`와 동일한 "실패를 전체 파이프라인이 멈추는 원인으로 만들지 않는다"는 원칙을 검색 단계에도 적용.
- **검증**: `py_compile`/import 확인 완료. 실제로 결과 0건 상황에서 크래시 없이 정상적으로 진행되는지는 사용자 재실행 검증 필요.
- **한계**: 예외 종류를 구분하지 않고 모두 빈 리스트로 처리하므로, 네트워크 연결 자체가 끊긴 경우와 "검색 결과가 정말 없는 경우"를 구분하지 못한다 — 둘 다 "검색 실패" 로그만 남고 동일하게 취급됨. 당장은 파이프라인이 안 죽는 것이 우선이라 이 정도로 충분하다고 판단.
- **보강 방향**: 재실행 시 크래시 없이 끝까지 도는지 확인. 참고로 같은 재실행에서 경쟁사비교 에이전트 쪽은 사용자가 직접 Ctrl+C로 중단한 것으로, 코드 버그가 아님.

---

## 18. 심층조사 가격 질의 전멸 원인 확인 및 수정 (2026-07-21)

- **발견 경위**: 16절 수정(회사명 정규화) 반영 후 재실행한 결과, 회사명 정규화 자체는 로그로 확인됐으나(`애플워치 (검색은 실제 회사명 'Apple'으로 정규화)` 등) 5개 경쟁사 전부 가격이 여전히 "(정보 없음)"이었다(단, Apple Watch Ultra·Redmi Watch는 시장조사 단계에서 우연히 수집된 fact 덕분에 프로필에 가격 문구 일부가 채워짐 — 심층조사 자체의 성과는 아님). 사용자가 브라우저로 "핏비트 가격", "삼성 갤럭시 워치 가격"을 직접 검색해보니 구글 AI 개요로 즉시 가격이 나오는 것과 대비되어, 왜 파이프라인은 못 찾는지 질문함.
- **원인 진단**: (1) 사용자가 보여준 검색 결과는 다나와·SSG·11번가 등 **국내 쇼핑몰의 원화(KRW) 가격**이라, 애초에 북미(target_market)에는 안 맞는 자료였음 — 지난 12/13절의 원화 오염 문제와 동일한 함정. (2) 구글의 "AI 개요"는 구글 자체 인덱스+쇼핑 데이터를 구글 LLM이 요약하는 기능으로, 지금 쓰는 `ddgs`(DuckDuckGo 기반) 텍스트 검색과는 완전히 다른 백엔드. (3) 공식 홈페이지·쇼핑몰 페이지는 가격을 자바스크립트로 렌더링하는 경우가 많아, 지금의 `requests.get()` + 정규식 태그 제거 방식(`fetch_page_text()`)으로는 애초에 가격 숫자가 안 보일 가능성. (4) 가격 질의 자체("{name} 가격 공식 홈페이지 제품")가 여러 의도가 뭉친 한국어 복합 질의라, 정작 가격이 정리된 페이지를 정확히 못 짚었을 가능성.
- **검토한 대안과 선택**: 헤드리스 브라우저(Playwright 등)로 JS 렌더링 결과를 직접 읽는 방법도 검토했으나, (a) 페이지 1건당 3~10초로 속도가 크게 느려지고 (b) Best Buy·Amazon 등은 봇 차단(CAPTCHA)이 강해 렌더링해도 막힐 가능성이 있으며 (c) 새 무거운 의존성(브라우저 바이너리 ~300MB)이 필요해, 2주 MVP 일정에는 리스크 대비 이득이 작다고 판단. 대신 사용자와 논의 후 "가격 비교 리뷰 기사 타겟팅"으로 결정 — "best smartwatch 2026"류의 정리 글은 대체로 정적 HTML로 서버 렌더링되고 봇 차단도 덜하며, 이미 여러 제품 가격을 표로 정리해두는 경우가 많음.
- **수정 내역**: `_deep_dive_queries()`의 가격 질의를 `"{name} 가격 공식 홈페이지 제품"`(한국어, 공식 홈페이지 타겟)에서 `"{name} price review comparison 2026"`(영어, 비교 리뷰 기사 타겟)으로 변경. 목표시장이 북미이고 이런 비교 리뷰 기사 자체가 영어권에 훨씬 많다는 점을 근거로 영어 질의를 채택. 이에 맞춰 `PRODUCT_TO_COMPANY_MAP`의 값도 한글 정식명("삼성전자") 대신 영어권 리뷰 기사에 실제로 쓰이는 이름("Samsung")으로 통일해, 검색과 fact 귀속(attribution) 양쪽에 일관되게 쓰이도록 함.
- **검증**: `_deep_dive_queries('Apple')`, `_company_name_for_deep_dive('삼성 갤럭시 워치')` 등을 직접 호출해 새 질의 문구·정규화 값을 확인. `py_compile`/import 완료. 실제로 가격 정보가 채워지는지는 사용자 재실행 검증 필요.
- **한계**: 투자·매출(`"{name} 투자 유치 매출 실적 규모"`) 질의는 이번엔 손대지 않음 — "투자 유치"(스타트업 VC 펀딩 표현)와 "매출 실적"(상장사 표현)이 한 질문에 뭉쳐 있어 Apple 같은 대기업에도 Sleepiz 같은 스타트업에도 어정쩡하게 안 맞는 문제가 여전히 남아있음. 사용자가 제안한 "증시 확인"(주가·시가총액 등 실제 금융 데이터)은 지금 구조에서는 웹검색만으로 대체하기 어렵고, `yfinance` 같은 별도 금융 데이터 라이브러리 연동이 필요한 별개의 작업으로 남겨둠.
- **보강 방향**: 재실행 후 가격 공란이 실제로 줄었는지 확인. 상장사/비상장사를 구분해 투자매출 질의를 이원화하는 작업은 다음 우선순위로 고려.

---

## 19. RAG 코퍼스(TTA/NCS)를 검증·출처 에이전트에 연결 (2026-07-21)

- **배경**: 2주 MVP 일정 점검(통합기획문서 5장) 중, `rag/` 디렉토리에 이미 TTA 표준문서 4건 + NCS 학습모듈 6건이 파싱·임베딩되어 Chroma DB(`rag/chroma_db`)로 구축돼 있는데, 어느 에이전트에서도 호출되고 있지 않다는 것을 확인함(제가 최초 점검 때 이 디렉토리 자체를 검색에서 빠뜨려 "RAG 미착수"로 잘못 보고했다가 사용자가 지적해 바로잡음).
- **역할 정리**: 이 코퍼스는 시장조사의 메인 소스(그건 웹검색)가 아니라, "이 보고서의 형식·분석 절차가 공인된 표준을 따르고 있는가"를 뒷받침하는 **출처 신뢰성 보강** 역할(통합기획문서 4장 원문 표현)이다. Fact 하나하나마다 조회하는 게 아니라, 문서를 최종 조립하는 단계에서 방법론적 근거가 필요할 때만 한두 번 조회하는 용도로 설계돼 있었음.
- **수정 내역**: `agents/verification.py`에 `get_methodology_citation()`(rag/query.py의 `search_corpus()` 재사용), `format_methodology_citation()`, 그리고 구체적 진입점 `cite_fact_metadata_standard()`(Fact 스키마의 출처 메타데이터 필드 구성 근거를 TTA 코퍼스에서 조회), `cite_pestel_methodology()`(PESTEL 분석 절차 근거를 NCS 코퍼스에서 조회) 추가. 조회 실패 시(코퍼스 접근 오류 등) 크래시 대신 "근거를 찾지 못함"으로 처리하는 이중 안전장치도 `search_web()`과 동일한 원칙으로 적용.
- **검증**: `py_compile`/import 확인 완료. 다만 실제 호출 테스트 중 이 작업환경(마운트된 폴더)에서 Chroma의 SQLite 접근이 `disk I/O error`로 실패하는 것을 확인 — 이는 코드 버그가 아니라 네트워크 마운트 파일시스템에서 SQLite 파일 잠금이 정상 동작하지 않는 환경적 한계로 판단됨(Chroma DB는 사용자 로컬 맥에서 생성됐으므로 로컬 환경에서는 정상 동작할 가능성이 높음). 에러 발생 시 크래시 없이 "근거 없음"으로 정상 처리되는 것까지는 확인함. 사용자 로컬 재실행 검증 필요.
- **한계**: 아직 이 함수들을 실제로 호출해서 최종 문서에 삽입하는 로직(Writer 에이전트, 검증·출처 에이전트의 문서합성 단계)이 없어, 지금은 "연결 지점만 마련된" 상태다 — 실제 사용은 Writer 에이전트 구현 시점에 이뤄짐.
- **보강 방향**: 로컬에서 `cite_fact_metadata_standard()`/`cite_pestel_methodology()` 직접 호출해 실제 코퍼스에서 근거가 조회되는지 확인. 이후 Writer 에이전트 설계 시, 방법론 섹션에 이 인용을 삽입하는 지점을 구체화.
- **재검증(2026-07-21, 클린 재실행)**: 다시 초기화 후 재실행한 결과, 시장조사 85개/경쟁사비교 34개 fact가 신규 저장됐고, 로그 전체를 확인한 결과 이전에 관찰됐던 한국 시장(식약처·한국소비자원 등)·카테터 등 무관한 fact가 이번엔 하나도 섞이지 않음을 확인함 — 관련성 필터 수정이 실제로 효과가 있었음. 경쟁사 25개가 식별됐고, 상당수(Apple/삼성전자/Google 등)는 가격을 제외한 핵심기능·타깃고객·강점·약점이 꽤 구체적으로 채워짐(recall 개선이 프로필 품질에도 실질적으로 기여). 다만 완전성 경고에서 "Whoop"가 여전히 후보에서 누락된 게 확인됨 — fact에 "WHOOP 스트랩은 운동 피로·회복 데이터 분석에 특화..."라는 내용이 실제로 저장돼 있었는데도 `identify_competitors()`가 별도 경쟁사로 뽑지 않음. 11-2절 recall 문제(있는데 놓침)가 완전히 해결된 건 아니고, 이번엔 25개 중 1개 정도로 발생 빈도가 크게 줄었을 뿐이라는 정도로 평가함. 그 외 완전성 경고에 뜬 "Apple/Samsung/Galaxy/Fitbit/가민" 등은 이미 후보에 한글명("삼성전자", "Fitbit")으로 존재하는데 영문 표기와 매칭이 안 된 것이라 11-2절에서 기록한 거짓 양성 한계가 그대로 재현된 것.
- **재검증(RAG 연결, 사용자 로컬 실행, 2026-07-21)**: 사용자가 로컬 맥에서 `python -c "from agents.verification import cite_fact_metadata_standard, cite_pestel_methodology; ..."`를 직접 실행해, 샌드박스에서 막혔던 `disk I/O error` 없이 두 함수 모두 정상 동작함을 확인함. `cite_pestel_methodology()`는 NCS "정보기술전략 R&D 전략 수립" 문서에서 "3C·5Force·SWOT·4P·7S·**STEEP**" 같은 환경분석 기법을 직접 인용해왔는데, STEEP은 PESTEL과 사실상 동일한 프레임워크라 PESTEL 분석 절차의 근거로 쓰기에 적합한 인용이었음. 반면 `cite_fact_metadata_standard()`가 조회한 TTAK.KO-10.1595는 실제로는 AI 학습용 데이터셋 메타데이터 표준이라 "데이터세트 버전/변경 이력" 같은 항목이 나왔는데, Fact 스키마의 출처 필드(source_url/retrieved_date/source_tier)를 직접 정당화하기엔 결이 다소 어긋남 — 주제(메타데이터 구성)는 겹치지만 필드 단위로는 정확히 대응되지 않는 느슨한 인용. RAG 연결 자체(코드 동작)는 성공으로 최종 확인하고, 인용 품질 개선은 별도 저우선 과제로 남겨둠(Writer 에이전트 설계 시 재검토 예정).

---

## 20. LangGraph Orchestrator 1차 구현 — research → PESTEL/경쟁사비교 병렬 실행 (2026-07-21)

- **배경**: 지금까지 시장조사·PESTEL·경쟁사비교 세 에이전트는 각각 독립적인 CLI 스크립트(`python -m agents.xxx`)로, 사람이 순서대로 하나씩 실행해야 했다. 실제로는 공유 Fact Store(SQLite)를 통해 간접적으로 이어져 있었지만(시장조사가 저장한 fact를 PESTEL·경쟁사비교가 `list_facts()`로 재조회), 실행 순서·재시도·에러 처리는 전부 수동이었다. PRD의 Orchestrator(LangGraph) 항목을 착수하면서, 사용자와 논의 후 이번 1차 구현 범위는 Writer·Human Checkpoint를 뺀 "research → PESTEL/경쟁사비교 병렬 실행"까지로 확정함(Writer·Human Checkpoint는 아직 로직 자체가 없어 별도 작업으로 남김).
- **설계**:
  - **State**: `PlanningState`(TypedDict) — `topic`, `target_market`, `facts`(시장조사 결과 Fact 리스트), `market_sizing`(MarketSizing), `pestel_summaries`(list[dict]), `competitors`(list[Competitor]), `errors`(Annotated[list[str], operator.add] — PESTEL·경쟁사비교 두 노드가 같은 스텝에서 동시에 에러를 추가할 수 있으므로 리듀서 필요).
  - **그래프**: `START → research → {pestel, competitor}(팬아웃) → join → END`. PESTEL·경쟁사비교는 서로 다른 state 키(`pestel_summaries` vs `competitors`)에만 쓰므로 동시 실행에 충돌이 없고, 이미 Fact Store만 공유하는 독립 로직이라 병렬 실행이 자연스럽다.
  - **에러 처리**: 각 노드를 try/except로 감싸 예외 발생 시 그래프 전체를 죽이지 않고 `errors`에 기록한 뒤 다음 단계로 진행 — `search_web()`/`get_methodology_citation()`에 적용했던 것과 동일한 "이중 안전장치" 원칙.
  - **CLI**: 기존 에이전트들과 동일하게 `python -m orchestrator.graph "<연구대상>" "<목표시장>"`로 실행.
- **구현 파일**: `orchestrator/__init__.py`, `orchestrator/graph.py`.
- **검증**: `py_compile`/import 확인 예정, 실제 그래프 실행은 사용자 로컬 재실행 필요.
- **한계**: Writer(기획서초안)·Human Checkpoint는 아직 그래프에 포함되지 않음 — PRD Must 항목이지만 로직이 없어 이번 범위에서 의도적으로 제외. 또한 pestel/competitor 노드는 여전히 각자 내부에서 `list_facts()`로 DB를 다시 읽으므로, `research` 노드가 반환한 `facts`를 state로 넘겨받고도 실제로는 활용하지 않는 상태(추후 필요시 in-memory 전달로 최적화 가능하나, DB가 이미 단일 진실 공급원 역할을 하므로 지금은 중복 조회 비용보다 일관성을 우선함).
- **보강 방향**: Writer 에이전트 구현 시 `join` 다음에 `writer` 노드를 추가하고, Human Checkpoint는 그 사이에 삽입하는 형태로 확장.

---

## 21. Writer 에이전트 1차 구현 — 기획서 초안 조립 (2026-07-21)

- **배경**: 아키텍처 다이어그램 대조(20절 논의 연장) 후, Market Sizing 분리·자동 삼각검증 게이트는 2주 MVP 범위에서 제외하고 Writer Agent를 다음 우선순위로 정함. 지금까지 4개 에이전트(시장조사·PESTEL·경쟁사비교·검증출처)의 결과물이 각자 흩어져 있어, 이걸 하나의 기획서로 조립하는 단계가 없으면 발표에서 보여줄 최종 산출물이 없는 상태였다.
- **설계 원칙**: Writer는 새 웹검색·새 fact 생성을 하지 않는다 — 순수 조립 단계. 시장조사·PESTEL·경쟁사비교에서 반복적으로 겪은 "출처 조작" 문제를 반복하지 않기 위해, 섹션별로 LLM 호출을 나눠서 각 섹션에는 필요한 데이터만 넘긴다(예: 경쟁사 포지셔닝 서술에는 경쟁사 프로필만 주고 PESTEL 내용은 안 줌). 유일한 예외인 "종합 시사점" 섹션도 원본 fact가 아니라 이미 조립된 섹션 텍스트만 입력으로 받고 "새 사실 지어내지 말 것" 프롬프트 제약을 건다(PESTEL/검증 에이전트와 동일 원칙).
- **구현**: `agents/writer.py` 신규 작성.
  - 섹션: ①개요+핵심요약(LLM, 통계만 근거) ②시장규모(템플릿) ③PESTEL(템플릿+`cite_pestel_methodology()`) ④경쟁사(마크다운 표+LLM 포지셔닝 서술) ⑤종합시사점(LLM, 위 섹션 텍스트만 재구성) ⑥출처부록(fact별 출처URL·신뢰도·검증상태+`cite_fact_metadata_standard()`)
  - `run_writer(topic, target_market, market_sizing=None, pestel_summaries=None, competitors=None)` — Orchestrator가 같은 실행에서 state를 넘겨주면 그대로 쓰고, 없으면(단독 CLI 실행) Fact Store에서 재조회(`latest_market_sizing()`, `list_competitors()`)하거나 PESTEL은 재실행(`run_pestel_analysis()` — 새 웹검색 없이 저장된 fact 재태깅이라 비용 크지 않음)한다.
  - 결과물은 마크다운 문자열로 반환하고, `multi-agent-system/outputs/{연구대상}_{목표시장}_기획서초안.md`로 파일 저장.
  - CLI: `python -m agents.writer "<연구대상>" "<목표시장>"`.
- **검증**: `py_compile`/import 확인, `_format_market_sizing_md`/`_collect_source_fact_ids`/`_slugify` 등 순수 함수는 샘플 데이터로 직접 호출해 출력 확인 완료. LLM 호출이 들어가는 함수(`write_executive_summary` 등)는 사용자 로컬 재실행 검증 필요.
- **한계**: 아직 Orchestrator 그래프(`orchestrator/graph.py`)의 `join` 뒤에 연결되지 않은 상태 — Human Checkpoint를 먼저 그 사이에 넣기로 한 순서(20절)에 따라, 그래프 연결은 Human Checkpoint 구현 이후로 미룸. `.docx` 변환도 아직 없음(마크다운만).
- **보강 방향**: Human Checkpoint 구현 후 `join → human_checkpoint → writer → END`로 그래프 연결. 이후 필요시 마크다운을 `.docx`로 변환하는 단계 추가 검토.

---

## 22. 경쟁사 프로필 구조화 단계, 배치 처리 없이 한 번에 다 넣어 응답 JSON이 잘리는 문제 (2026-07-21 발견·수정)

- **발견 경위**: Orchestrator(`orchestrator/graph.py`)를 리셋 없이 끝까지 실행한 결과, PESTEL 노드는 6축 요약까지 정상 완료됐으나 경쟁사비교 노드에서 `JSONDecodeError: Unterminated string starting at: line 3733`이 발생하며 실패함(Orchestrator의 에러 처리는 의도대로 동작 — 이 노드만 실패하고 전체 그래프는 `join`까지 도달함).
- **원인**: 이번 실행에서 fact가 309개까지 쌓이면서 `identify_competitors()`가 경쟁사 후보를 25개 가까이 식별했는데, `build_competitor_profiles()`는 후보 전체를 **LLM 호출 한 번**으로 처리하도록 짜여 있었다. 경쟁사마다 가격·기능·타깃·채널·투자매출·강점·약점을 다 채우다 보니 응답 JSON이 모델의 출력 토큰 한도를 넘어 문장 중간에서 잘렸고, `json.loads()`가 실패함. PESTEL의 `tag_facts()`/`summarize_by_axis()`도 동일하게 "한 번에 전체 배치"하는 구조였는데, 이번엔 규모(축 6개·fact 264개)가 우연히 한도 안에 들어와 안 걸렸을 뿐, 같은 구조적 위험을 안고 있었음.
- **수정 내역**:
  - `agents/competitor.py`: `PROFILE_BATCH_SIZE = 8` 도입, `build_competitor_profiles()`가 후보를 8개씩 나눠 여러 번 호출하고 결과를 합치도록 수정.
  - `agents/pestel.py`: `TAG_BATCH_SIZE = 40` 도입, `tag_facts()`가 fact를 40개씩 나눠 배치 태깅. `summarize_by_axis()`는 `_bin_pack_axis_blocks()`(신규)로 축 그룹을 `SUMMARY_MAX_FACTS_PER_CALL = 40` 기준 총합이 넘지 않게 여러 배치로 묶어 호출하고, 한 축이 그 자체로 임계값을 넘으면 그 축만 따로 쪼개 여러 번 호출한 뒤 narrative를 이어붙이고 fact id 목록을 합쳐 하나의 요약으로 반환.
- **검증**: `py_compile`/import 확인. `_chunked()`/`_bin_pack_axis_blocks()`는 순수 함수라 가짜 데이터로 직접 호출해 동작 확인(축 하나가 임계값을 넘는 경우 정상적으로 쪼개지는 것 확인). 실제 LLM 호출이 들어가는 배치 분기(`build_competitor_profiles`/`tag_facts`/`summarize_by_axis`)는 사용자 로컬 재실행 검증 필요.
- **한계**: 배치 크기(8/40)는 이번 실행 규모를 기준으로 정한 경험적 값이라, 경쟁사 하나·fact 하나의 정보량이 유난히 많은 경우(예: 심층조사로 fact가 20개 이상 배정된 경쟁사)에는 여전히 한 배치 안에서 잘릴 여지가 있음 — 완벽한 상한은 아니고, 문제가 재발하면 배치 크기를 더 줄이는 방향으로 대응.
- **보강 방향**: 로컬에서 이번과 같은 규모(fact 300개 이상, 경쟁사 25개 이상)로 재실행해 배치 처리 후 정상적으로 프로필이 다 채워지는지 확인.
- **재검증(2026-07-21) 및 2차 발견**: 사용자가 리셋 없이 Orchestrator를 두 번 더 재실행함. PESTEL 쪽 배치 처리는 정상 동작 확인됨 — 로그에서 Economic·Social·Technological 축이 여러 개의 narrative 문단으로 이어붙어 나오는 것을 확인했는데, 이는 `_bin_pack_axis_blocks()`가 그 축의 fact가 너무 많아 여러 배치로 쪼갠 뒤 `summarize_by_axis()`가 문단을 합친 결과다(의도한 동작). 그런데 경쟁사비교 노드는 **같은 종류의 에러(`JSONDecodeError: Unterminated string`)로 재차 실패**함. 원인을 다시 보니 `build_competitor_profiles()`는 고쳤지만, **그 앞 단계인 `identify_competitors()`를 놓쳤음** — 이 함수는 fact 전체(이번엔 455개)를 한 번의 LLM 호출로 처리하는데, fact가 많아지면 경쟁사마다 `mentioned_fact_ids` 배열이 길어져 똑같은 이유로 응답이 잘림. 로그에 "경쟁사 후보 N개 식별:" 출력이 아예 안 찍힌 것으로 이 단계에서 실패했음을 확인.
- **추가 수정**: `IDENTIFY_BATCH_SIZE = 60` 도입, `identify_competitors()`가 fact를 60개씩 나눠 여러 번 호출하고, 같은 이름(대소문자 무시)의 경쟁사는 `mentioned_fact_ids`를 합쳐 하나로 병합하도록 수정. 배치가 나뉘면서 같은 회사가 다른 표기(예: "삼성" vs "삼성전자")로 식별되면 병합이 안 되고 별도 항목으로 남을 수 있다는 한계는 11-2절의 기존 한계와 동일한 종류라 별도로 문서화함.
- **검증**: `py_compile` 확인 완료. 실제 LLM 호출 재검증은 사용자 로컬 재실행 필요.

## 23. 경쟁사 엔티티 중복(애플/애플워치/애플 헬스킷) 병합 (2026-07-22)

- **재검증 결과**: 22절의 배치 처리 수정(IDENTIFY_BATCH_SIZE=60) 이후 로컬 재실행에서 `JSONDecodeError`가 재발하지 않고 오케스트레이터가 오류 없이 완주함(fact 73개, PESTEL 5개 축, 경쟁사 프로필 22개). 배치 처리 버그는 이것으로 재검증 완료.
- **새로 발견한 문제**: 경쟁사 후보 22개 중 "애플"·"애플워치"·"애플 헬스킷"이 실제로는 같은 회사인데 3개의 별도 항목으로 잡힘. "가민"/"Garmin", "오우라 링"/"Oura", "삼성 갤럭시 워치"/"Samsung"도 같은 종류의 잠재적 중복.
- **원인**: `identify_competitors()` 프롬프트의 "서로 다른 기업/브랜드명이 언급될 때마다 빠짐없이 별도 항목으로 포함하라"는 완전성(recall) 지시가, 제품명·서비스명·플랫폼명까지 전부 "다른 브랜드명"으로 취급해 같은 모기업을 여러 항목으로 쪼개는 부작용을 냄. 기존 `PRODUCT_TO_COMPANY_MAP`(회사명 정규화 매핑)은 심층조사 검색 질의 생성에만 쓰이고 식별 단계 병합에는 적용되지 않고 있었음.
- **수정**: 이중 안전장치로 대응.
  1. 프롬프트에 "같은 모기업의 제품·서비스·플랫폼명은 모기업명 하나로 통합하라"는 지시 추가 (기준을 "표현이 다른지"가 아니라 "다른 회사인지"로 명시).
  2. `identify_competitors()`의 병합 로직을 `_canonical_company_name()`(기존 `_company_name_for_deep_dive()`를 이름 변경·용도 확장)으로 이름을 정규화한 뒤 그 키로 병합하도록 수정. `PRODUCT_TO_COMPANY_MAP`에 "애플 헬스킷", "애플", "삼성", "구글", "핏빗", "가민", "오우라"/"오우라 링" 등 회사 기본명·한글 표기 항목을 추가로 등록.
- **검증**: `py_compile` 확인. `_canonical_company_name()` 및 병합 로직을 더미 후보 리스트로 직접 호출해 "애플"/"애플워치"/"애플 헬스킷"이 하나의 "Apple" 항목으로 병합되고 `mentioned_fact_ids`가 합쳐지는 것을 확인함. 실제 LLM 호출(프롬프트 지시 반영 여부)은 사용자 로컬 재실행 검증 필요.
- **한계**: `PRODUCT_TO_COMPANY_MAP`에 없는 새로운 제품명·표기(예: 아직 등록 안 된 브랜드의 신제품명)는 여전히 별도 항목으로 남을 수 있음 — 11-2절/22절에 이미 기록된 것과 같은 종류의, 완벽할 수 없는 휴리스틱 한계.

## 24. Writer → Orchestrator 그래프 연결 (2026-07-22)

- **작업 내용**: `orchestrator/graph.py`에 `writer_node` 추가, `PlanningState`에 `draft_markdown: Optional[str]` 필드 추가, 그래프 엣지를 `join → writer → END`로 변경(기존 `join → END`에서 확장). `run()`의 초기 state에도 `draft_markdown: None` 추가.
- **설계 포인트**: `writer_node`는 `run_writer()`를 호출하되, `state.get("market_sizing")`/`state.get("pestel_summaries")`/`state.get("competitors")`를 그대로 넘기지 않고 `or None`으로 감싸서 넘긴다. 이유: 앞선 노드(예: competitor_node)가 실패하면 해당 state 키는 초기값(빈 리스트)으로 남는데, `run_writer()`는 인자가 `None`일 때만 Fact Store에서 재조회하는 폴백 로직을 갖고 있다(`agents/writer.py`의 `run_writer()` 설계). 빈 리스트를 그대로 넘기면 "이번 실행에 결과가 없다"와 "인자를 안 줬다"를 구분 못 해 폴백이 발동하지 않으므로, falsy 값을 명시적으로 None으로 바꿔 넘기도록 했다.
- **검증**: `py_compile` 통과. `build_graph()`를 호출해 노드·엣지 구조를 출력해 `START→research→{pestel,competitor}→join→writer→END`가 의도대로 만들어졌는지 확인함(LLM 호출은 하지 않는 구조 검증). 실제 로컬 실행(Writer의 LLM 호출 포함)은 사용자 재실행 검증 필요.
- **남은 작업**: Writer 섹션 제목을 KOSENA 용어로 정리하는 작업(별도 task)은 아직 미착수.

## 25. 기획서 초안 산출물 검토 후 2건 추가 수정 (2026-07-22)

- **계기**: Writer→Orchestrator 연결 후 실제로 생성된 `웨어러블_헬스케어_기기_북미_시니어_건강관리_시장_기획서초안.md`를 검토한 결과, 두 가지 개선점을 발견함.
- **1) 경쟁사 엔티티 중복 재발**: 23절에서 고친 "애플/애플워치/애플 헬스킷" 케이스는 사라졌으나, "Apple Inc"/"Apple", "OMRON Healthcare, Inc."/"오므론", "Koninklijke Philips N.V"/"필립스"처럼 `PRODUCT_TO_COMPANY_MAP`에 없는 새로운 표기 변형으로 같은 문제가 재발함. 하드코딩 사전을 계속 넓히는 방식의 구조적 한계로 판단.
  - **수정**: `identify_competitors()`의 배치 병합이 끝난 뒤, 최종 후보 리스트 전체를 한 번에 LLM(`solar-pro2`)에 보여줘 "표기만 다를 뿐 같은 회사인 것"을 그룹으로 묶게 하는 `_consolidate_duplicate_competitors()`를 추가함. LLM이 목록의 이름을 그룹에서 빠뜨릴 경우를 대비해, 코드 레벨에서 누락된 이름을 단독 항목으로 그대로 추가하는 이중 안전장치를 뒀다.
  - **검증**: `py_compile` 확인. 가짜(mock) OpenAI 클라이언트로 병합 로직만 단위 테스트 — (a) 정상 그룹핑 시 fact_id가 합집합으로 합쳐지는 것, (b) LLM이 일부 이름을 그룹에서 빠뜨려도 단독 항목으로 안전하게 남는 것 두 시나리오 모두 확인함. 실제 LLM 호출(정말 같은 회사인지 잘 판단하는지)은 사용자 로컬 재실행 검증 필요.
- **2) TAM 기준 연도 문제**: `calculate_market_sizing()`이 TAM을 미래 예측 종료 연도(예: 2031년) 값으로 잡아, "현재 시장 크기"가 아니라 "미래 잠재력"을 보여주는 셈이 되는 정의상 문제 발견.
  - **수정**: `agents/market_research.py`의 `_estimate_topdown()` 프롬프트에 "여러 연도 수치가 있으면 미래 예측 종료 연도가 아니라 가장 최근 실측(또는 실측에 가까운) 연도를 우선 사용하라"는 지시 추가. `MARKET_SIZING_SCHEMA`의 `tam_usd`/`tam_year` 필드 설명에도 같은 원칙과, 부득이하게 예측치를 쓸 경우 `tam_year`에 "(예측치)"를 표기하라는 지시를 추가함.
  - **검증**: 프롬프트·스키마 문구 수정이라 `py_compile`만 확인. 실제 LLM이 이 지시를 따라 최근 연도를 우선하는지는 사용자 로컬 재실행 검증 필요.
- **한계**: 엔티티 통합 LLM 패스도 결국 LLM 판단에 의존하므로, 아주 생소한 신생 기업명(모기업 관계가 잘 알려지지 않은 경우)은 여전히 잘못 나뉘거나 잘못 합쳐질 수 있음 — 완벽한 해법이 아니라 하드코딩 사전보다 한 단계 더 나은 근사치.

## 26. DOCX 내보내기 구현 + RAG 방법론 근거 섹션 제거 (2026-07-22)

**배경**: 체크포인트3("제공된 기획방법론 템플릿(KOSENA 등) 준수, DOCX/PPTX 내보내기 지원")과
사용자가 업로드한 KOSENA 강의자료(제출 형식: 본문 PDF/DOCX, A4)를 근거로 마크다운(.md)만
내던 Writer 에이전트에 DOCX 출력을 추가함.

- **RAG 방법론 근거 섹션 제거**: 기존 Writer는 3절(PESTEL) 밑에 "방법론 근거", 6절(출처 부록)
  밑에 "출처 메타데이터 구성 근거"를 넣고 있었는데, 이건 `cite_pestel_methodology()`/
  `cite_fact_metadata_standard()`가 RAG 코퍼스(NCS/TTA 표준 PDF)에서 검색된 청크를 LLM
  재구성 없이 그대로 붙여넣는 방식이라, 실제 산출물(투자자/심사자가 읽는 기획서)에 어울리지
  않는 표준 문서 원문(예: 일반 IT전략기획 문구)이 섞여 들어가는 문제가 있었음. 사용자 지적으로
  두 섹션을 기획서 본문에서 제거하고, 두 함수는 `verification.py`에 그대로 남겨 필요 시
  (예: 멘토링 질의응답 자료) 별도로 호출하도록 변경.
- **DOCX 내보내기**: 새 모듈 `agents/docx_export.py::export_to_docx()` 추가.
  마크다운 문자열(표 포함)을 다시 파싱해서 Word로 되돌리는 방식은 깨지기 쉬워서 피하고,
  `run_writer()`가 이미 들고 있는 구조화된 객체(`MarketSizing`, `pestel_summaries`,
  `Competitor` 리스트, exec_summary/positioning/synthesis 텍스트)를 그대로 받아 Word 요소
  (제목, Heading 1~3, 불릿, 진짜 Word 표)로 직접 조립함. A4 용지(python-docx 기본값인
  US Letter를 명시적으로 재설정), 여백 2.5cm로 KOSENA 제출 형식 준수.
  `run_writer()`는 `.md`(내부 검토용)와 `.docx`(제출용)를 같은 파일명 base로 둘 다 저장.
- **검증**: `py_compile` 통과. 이 샌드박스는 외부망 제한으로 Upstage API 호출이 안 돼
  PESTEL 재태깅을 포함한 전체 `run_writer()` 실행은 못 했지만, 기존 fact_store.db에 저장된
  실제 market_sizing/competitors 데이터(2026-07-22 실행분)로 `export_to_docx()`를 직접
  호출해 DOCX를 생성하고, LibreOffice(`soffice.py`)로 PDF 변환 후 페이지 이미지를 직접 확인함
  — 표지, 목록, 표(굵은 헤더), PESTEL 소제목, 출처 부록 모두 A4 규격으로 정상 렌더링됨.
- **남은 것**: PPTX 내보내기는 이후 별도 작업(할일 목록 #4). `requirements.txt`에
  `python-docx>=1.1.0` 추가함.

## 27. 경쟁사 표 가독성 문제 — 유형별 상한 도입 (2026-07-23)

**배경**: 실제 생성된 DOCX 기획서(`웨어러블_헬스케어_기기_북미_시니어_건강관리_시장_기획서초안.docx`)를
사용자가 직접 열어보고 4절(경쟁사 분석) 표가 3페이지에 걸쳐 20개 회사를 나열해 가독성이
떨어진다고 지적함. 렌더링해서 확인한 결과, "핵심 기능" 컬럼이 너비 지정 없이 다른 컬럼과
균등 분배되면서 세로로 6~8줄씩 늘어나는 문제와, 정보가 거의 없는 회사(잠재 진입자 7개,
간접 경쟁자 4개 — 대부분 근거 fact 1건)가 표를 채우는 문제 두 가지가 겹쳐 있었음.

**1차 시도(기각)**: "채워진 필드 개수가 2개 미만이면 제외"하는 기준을 제안했으나, 사용자가
"가격/투자매출/채널은 원래 대다수가 비어 있는데 이 기준으로 거르면 남는 회사가 없지 않냐"고
정확히 지적. 실제 데이터로 확인한 결과 price(6%)/channel(0%)/funding_or_revenue(12%)
채움률이 Apple·Samsung 같은 주요 회사조차 극히 낮아, 이 기준으로는 17개 회사 전부 필드
4~5개로 몰려 있어 아무것도 걸러지지 않는 것으로 확인됨(잘못된 기준이었음).

**최종 채택**: 근거 fact 개수(mentioned_fact_ids/source_fact_ids)를 1순위 기준으로 삼음 —
실제로 직접 경쟁자는 Fitbit(12건)~Garmin(1건)까지 뚜렷한 차이가 있었음. 동점 시(간접·잠재
진입자는 거의 전부 1건으로 동률) 2순위로 근거 fact의 verification_score 최고값, 3순위로
source_tier(1차>2차>3차) 최고값을 적용. 그래도 동점이면 Python 안정 정렬로 식별 원래
순서를 유지(의미는 없지만 실행 결과가 흔들리지 않게 하는 폴백).

- **구현**: `agents/competitor.py`에 `select_top_competitors()`/`_competitor_sort_key()`
  추가. 유형별 상한은 KOSENA 강의자료(M2.3절, "직접 3+간접 2+잠재 1")를 참고해
  `DIRECT_COMPETITOR_CAP=5`, `INDIRECT_COMPETITOR_CAP=3`, `POTENTIAL_COMPETITOR_CAP=2`로
  설정(KOSENA 권장보다 직접 경쟁자는 조금 여유를 둠 — 실제 근거량 분포상 5위 안에서도
  변별력이 있었음). `run_competitor_analysis()`에서 `identify_competitors()` 직후,
  `deep_dive_competitors()`/`build_competitor_profiles()` 이전에 호출해 이후 단계의
  검색·LLM 비용도 함께 줄임.
- **검증**: `py_compile` 통과. 기존 fact_store.db의 실제 경쟁사 17개(직접6/간접4/잠재7)로
  `select_top_competitors()`를 직접 호출해 10개(직접5/간접3/잠재2)로 줄어드는 것,
  동점 상황(간접·잠재는 전부 근거 1건)에서도 verification_score/source_tier로 실제
  차등이 발생하는 것(단순 원래 순서 유지가 아님)을 확인함.
- **남은 것**: 표 레이아웃 자체(컬럼 너비 명시, 유형별 표 분리)는 아직 미착수 — 
  `agents/docx_export.py`의 표 렌더링 개선 필요.

## 28. 경쟁사 표 레이아웃 개선 완료 (2026-07-23)

27절에서 회사 수는 17→10개로 줄였으나, 표 자체의 컬럼 폭·구조는 그대로였어서 마저 개선함.

- **컬럼 구조 변경**: `docx_export.py`의 경쟁사 표를 7컬럼(유형 포함) 균등폭에서, 유형별로
  소제목(직접 경쟁자/간접 경쟁자(대체재)/잠재 진입자) + 개별 표 3개로 분리하고 "유형" 컬럼
  자체를 제거해 6컬럼으로 줄임. 컬럼 너비를 명시적으로 지정(경쟁사 2.2cm/가격 2.3cm/
  핵심기능 5.5cm/타깃고객 2.5cm/채널 1.8cm/투자매출 1.7cm, A4 여백 제외 16cm에 배분) —
  이전엔 너비 지정이 없어 "핵심 기능"처럼 긴 텍스트가 좁은 균등폭에 눌려 세로로 6~8줄씩
  늘어나는 문제가 있었음.
- **셀 내부 표현 변경**: 핵심 기능을 세미콜론으로 이어붙인 한 줄 대신, 항목당 한 줄("- " 접두)
  로 나눠 담음(`_fill_cell_bullets()`).
- **검증**: `py_compile` 통과. `select_top_competitors()`로 추린 실제 10개 경쟁사로
  `export_to_docx()`를 호출해 LibreOffice로 PDF 변환 후 페이지 이미지 확인 —
  이전(20개, 균등폭 7컬럼)엔 표 하나가 3페이지를 꽉 채웠으나, 개선 후(10개, 유형별 분리
  6컬럼)엔 3개 표 합쳐서 약 1.5~2페이지로 줄고 각 유형이 소제목으로 명확히 구분됨.

## 29. PESTEL 축 정의를 프롬프트에 직접 삽입 (RAG 재료화, 2026-07-23)

PICO팀 비교에서 배운 "RAG를 장식용 인용이 아니라 분석 프롬프트 재료로 쓴다"는 아이디어(26절
`pico_architecture_lessons` 메모리 참고)를 적용함. 매번 실시간 벡터 검색을 태우는 대신, RAG
코퍼스 원본(`rag/parsed/03+정보기술전략+정보기술+R&D+전략+수립_....md` 1321~1333행,
<표 1-15> STEEP 프레임)에서 실제로 확인한 축별 정의를 `pestel.py`의 `AXIS_DEFINITIONS`
상수로 고정해서 씀 — 유사도 검색은 실행마다 같은 청크가 나온다는 보장이 없어, 검증된 내용을
확정값으로 두는 게 안정적이라고 판단.

- **5축→6축 재구성**: 원문 STEEP은 5축(Political/Legal 통합)이라 저희 6축 스키마에 안 맞음.
  "Political/Legal(정책/법규)" 항목(탈규제화·민영화·산업구조조정·시장자율 경쟁체제 전환·
  좌우파 정책성향과 이론)을 Political(정책 방향·이념·안정성)과 Legal(실제 시행되는 법·제도
  변화)로 나눔 — 일반 PESTEL 교재의 Political/Legal 구분 기준과 동일하게 해석. Ecological은
  이름만 Environmental로 바꾸고 내용은 그대로 대응.
- **구현**: `tag_facts()`(6축 분류 — 특히 Political/Legal 경계 판단에 직접 도움)와
  `summarize_by_axis()`(축별 narrative가 정의 범위를 벗어나지 않게 참고용) 두 프롬프트
  모두에 `AXIS_DEFINITIONS_BLOCK`을 삽입.
- **검증**: `py_compile` 통과, `AXIS_DEFINITIONS_BLOCK` 문자열이 6축 모두 올바른 정의로
  채워지는 것 확인. 다만 이 샌드박스는 Upstage API 호출이 막혀 있어, "실제로 축 분류
  정확도가 좋아졌는가"는 사용자 로컬 재실행으로 확인 필요 — 특히 Political/Legal 경계
  판정이 이전보다 일관되게 나오는지 눈여겨볼 것.

## 30. 경쟁사 질문 생성에 기존 fact 활용 (2026-07-23)

`generate_competitor_queries()`가 topic·target_market 문자열만 보고 백지에서 질문을 생성해,
시장조사 단계에서 이미 브랜드명이 언급된 fact가 있어도 전혀 참고하지 않는 문제를 해결함.

- **구현**: `_existing_facts_hint_block()` 추가 — 기존 fact(최대 `EXISTING_FACTS_HINT_CAP=30`개
  샘플)를 "참고자료" 블록으로 만들어 `generate_competitor_queries()`의 프롬프트에 삽입.
  이미 답 나온 걸 반복 질문하지 말고, 언급된 브랜드가 있으면 그 브랜드의 구체적 후속 질문
  (정확한 가격·시장점유율 등)을 우선하라고 지시함. `run_competitor_analysis()`에서 질문
  생성 전에 topic 일치 fact를 먼저 조회해서 넘겨줌(historical_facts 판정과 같은 topic
  매칭 로직 재사용, SQLite 로컬 조회라 두 번 호출해도 비용 부담 없음).
- **검증**: `py_compile` 통과. 실제 fact_store.db(topic="웨어러블 헬스케어 기기", 109개
  매칭)로 `_existing_facts_hint_block()`을 직접 호출해 30개 샘플이 정상적으로 참고자료
  블록(2,201자)으로 만들어지는 것 확인. 실제 LLM이 이 힌트를 보고 더 맞춤화된 질문을
  만드는지는 Upstage API 호출이 필요해 사용자 로컬 재실행 검증 필요.

## 31. deep_dive 가격 질의 언어를 목표시장에 맞게 동적으로 결정 (2026-07-23)

`_deep_dive_queries()`의 가격 질의가 "목표시장은 항상 북미"라는 전제로 영어("{name} price
review comparison 2026")를 하드코딩하고 있던 문제를 해결함.

- **구현**: `_ENGLISH_REVIEW_MARKET_KEYWORDS`(북미/미국/캐나다/영어권 등) 키워드 목록과
  `_use_english_price_query(target_market)` 판정 함수 추가. `_deep_dive_queries()`가
  `target_market`을 추가로 받아, 북미/영어권이면 기존처럼 영어 질의, 그 외(한국 국내 등)는
  한국어 질의("{name} 가격 비교 리뷰 추천 2026")로 분기. `deep_dive_competitors()`의
  호출부도 `target_market`을 넘기도록 수정.
- **한계**: 완벽한 언어 감지가 아니라 목표시장 표현 키워드 목록 기반 휴리스틱 — 목록에 없는
  새로운 지역 표현(예: "유럽", "일본")은 기본값(한국어)로 처리된다. 필요시 키워드 목록을
  확장하면 됨.
- **검증**: `py_compile` 통과. `_deep_dive_queries()`를 직접 호출해 목표시장별로 분기가
  정확히 되는 것 확인(북미→영어, 한국 국내→한국어, target_market 미지정 기본값→한국어).

## 32. 다중 검색 백엔드 추가 (Brave Search API, 2026-07-23)

recall 개선 4항목 중 마지막으로 남아있던 "ddgs 하나만 쓰는 문제"를 해결함(project_recall_priority
메모리 참고). ddgs가 결과를 충분히 못 주는 질의에서도 recall이 그대로 죽는 문제를 완화하기
위해, Brave Search API를 보조 백엔드로 추가.

- **구현**: `web_search.py`에 `_search_brave(query, max_results)` 추가 — Brave Search API
  (`https://api.search.brave.com/res/v1/web/search`)를 호출해 ddgs와 같은 형식(`title`/`url`/
  `snippet`/`content`/`source_tier`)으로 변환. `BRAVE_API_KEY`(.env)가 없으면 조용히 빈
  리스트를 반환해 기존 ddgs-only 동작과 동일하게 유지(선택적 기능).
  `search_web()`을 리팩터링해서: ddgs 결과가 `max_results`에 못 미치면(ddgs 자체가 예외로
  실패한 경우 포함) 부족한 만큼만 Brave로 보완, URL 기준 중복 제거, `fetch_full_text=True`면
  Brave로 얻은 결과도 동일하게 `fetch_page_text()`로 본문을 채움.
- **설계 이유**: Bing 검색결과 페이지 스크래핑(API 키 불필요)도 검토했으나, 이 샌드박스는
  외부망이 막혀 있어 Bing의 실제 HTML 마크업을 확인하고 정규식을 검증할 방법이 없었음 —
  검증 안 된 스크래핑 코드보다는, 사용자가 무료 API 키를 발급받아야 하는 수고가 있더라도
  공식 문서화된 안정적인 JSON API(Brave)를 택하는 게 더 신뢰할 수 있다고 판단(사용자 확인
  후 결정).
- **검증**: `py_compile` 통과. `BRAVE_API_KEY` 없을 때 `_search_brave()`가 빈 리스트를
  반환하는 것, 가짜 키로 호출 시 네트워크 예외를 크래시 없이 흡수하는 것, ddgs가 예외로
  실패해도 Brave 보완 시도로 계속 진행돼 `search_web()`이 빈 리스트를 반환하며 정상
  종료하는 것까지 확인함. 다만 이 샌드박스는 외부망이 막혀 있어 **실제 검색 결과가
  정상적으로 오는지는 사용자 로컬에서 확인 필요** — brave.com/search/api에서 무료 API
  키를 발급받아 `.env`에 `BRAVE_API_KEY=...`를 추가하면 활성화된다.

## 33. fetch_page_text() User-Agent 수정 (2026-07-23)

**원인**: 봇 탐지 문제를 논의하던 중, `fetch_page_text()`가 쓰던 User-Agent 문자열이
`"Mozilla/5.0 (compatible; ResearchBot/1.0)"`으로 스스로 "ResearchBot"이라고 봇임을
밝히고 있다는 걸 발견함. 많은 사이트의 1차 방어선은 정교한 탐지가 아니라 User-Agent에
"bot" 같은 단어가 있는지 보는 단순 필터라, 이건 불필요하게 차단을 자초하는 요인이었음.

**과정**: `PAGE_FETCH_USER_AGENT` 상수를 실제 크롬 브라우저가 보내는 것과 동일한 형태의
User-Agent 문자열로 교체하고, `Accept`/`Accept-Language` 헤더도 함께 추가해 실제 브라우저
요청과 더 비슷하게 만듦.

**결과**: `py_compile` 통과, `PAGE_FETCH_USER_AGENT`에 "bot" 문자열이 없는 것 확인.

**의의**: 저비용(코드 몇 줄)으로 단순 문자열 필터 수준의 불필요한 차단은 피할 수 있음.

**한계**: 이건 정교한 봇 탐지(navigator.webdriver 플래그, 데이터센터 IP 대역 차단, 헤드리스
브라우저 흔적 등)를 우회하는 게 아니라, "가장 단순한 1차 필터"만 통과하게 해주는 수준의
개선임. User-Agent만 바꾼다고 CAPTCHA나 정교한 봇 탐지 시스템까지 우회되는 건 아니며,
이런 정교한 우회(주거용 프록시 로테이션, CAPTCHA 자동 풀이 등)는 비용·정책상 이 프로젝트
범위에서 다루지 않기로 함 — 특히 CAPTCHA 우회는 정책상 금지된 행동이라 애초에 검토 대상이
아님.

## 34. capability_qa 검색 버그 — Chroma HNSW 근사검색이 소수 문서 컬렉션을 놓치는 문제 (2026-07-24)

**원인**: "이 서비스가 PPTX도 만들어주나요?" 같은 서비스 기능 질문에 `answer_capability_question()`이
계속 하드코딩 폴백 문구("...담당자에게 직접 문의해 주세요")로 답함. `capability_corpus.md`는
실존하고 `build_corpus` 로그도 CAPABILITY 청크 3개를 정상 추가했다고 보고했는데, 실제 조회
시점에는 검색이 안 되는 모순이 있었음.

**과정**: 재현 테스트로 원인을 확정함. 처음엔 무작위(random) 벡터로 재현을 시도해 "문제
없음"으로 잘못 결론 낼 뻔했으나, 실제 코퍼스 규모·비율(전체 1264개 청크 중 CAPABILITY
3개)에 맞춰 군집화(clustered)된 합성 벡터로 다시 테스트하니 재현됨 — Chroma의 HNSW 근사
최근접 검색이 대규모 공유 컬렉션 속 극소수 비중 문서는 `where` 필터를 걸어도 검색 후보군에
아예 못 들어가는 경우가 있음을 확인함. 진단 스크립트로 `.get()`(필터만, 근사검색 없음)으로는
3개 청크가 정확히 나오지만 `.query()`(근사 최근접 검색)로는 안 나오는 것을 대조해 최종 확정.

**결과**: `rag/build_corpus.py`에 `CAPABILITY_COLLECTION_NAME = "capability_corpus"` 전용
컬렉션을 추가하고, `build_capability_corpus()`가 기존 공유 컬렉션(`tta_ncs_corpus`)이 아니라
이 전용 컬렉션에 저장하도록 변경. `rag/query.py`에 `search_capability_corpus()` 신규 함수를
추가(전용 컬렉션만 조회). `agents/router.py`가 기존 `search_corpus()` 대신 이 함수를 쓰도록
교체.

**의의**: "필터로 거르면 되지 않냐"는 직관과 달리, 대규모 공유 벡터 컬렉션 안의 극소수 비중
특수 목적 문서는 필터만으로는 근본적으로 검색이 보장되지 않는다는 것을 재현으로 확인함 —
향후 비슷한 소규모 특수 코퍼스를 추가할 때는 공유 컬렉션+필터보다 전용 컬렉션 분리를 우선
고려해야 함.

**한계**: 전용 컬렉션은 지금처럼 코퍼스가 작을 때(청크 3개)는 문제가 없지만, 이 전용
컬렉션 자체가 나중에 커지면 같은 문제가 재발할 가능성이 있음 — "얼마나 커지면 다시
위험해지는가"의 임계값은 별도로 실증하지 않았음.

## 35. capability_qa 답변 품질 버그 — 명시적 부정 답변과 정보없음 미구분 (2026-07-24)

**원인**: 34절 수정 후에도 같은 질문에 여전히 "확인되지 않습니다"류의 답변이 나옴. 다만
문구를 자세히 대조해보니 하드코딩된 폴백 문자열과 정확히 일치하지 않아, 이번엔 검색 실패가
아니라 LLM이 직접 생성한 답변이라는 것을 구분해냄.

**과정**: `capability_corpus.md`에는 "PPTX는 아직 지원하지 않는다"는 명시적 부정 서술이
있고 검색도 이번엔 성공(청크 3개 정상 전달)했는데, `CAPABILITY_ANSWER_SCHEMA`의 description이
"답을 모르면 확인되지 않습니다라고 답하라"는 지시만 담고 있어, LLM(solar-mini)이 "명시적으로
안 된다고 나와 있는 것"과 "아예 정보가 없는 것"을 구분하지 못하고 후자로 처리한 것으로 판단함
(원인 확정은 아니고 정황 근거 — 폴백 하드코딩 문자열이 실제 출력에 없다는 점으로 LLM 자체
생성 답변임을 특정).

**결과**: `CAPABILITY_ANSWER_SCHEMA`의 `answer` description과 `answer_capability_question()`의
프롬프트 본문 양쪽에 "명시적 부정 설명도 확인된 답변"이라는 규칙을 구체적 예시와 함께 추가함.
사용자 실기기 재실행으로 "아직 PPTX 생성은 지원하지 않고 DOCX만 지원합니다"라는 올바른 답변을
확인함.

**의의**: 구조화 출력 스키마의 description 한 곳에만 미묘한 행동 규칙을 적어두면 가벼운
모델이 놓칠 수 있다는 것을 확인함 — 이 교훈은 이후 Router 설계(36절)와 경쟁사 필드
분류(39절)에도 동일하게 적용함.

**한계**: 이 수정도 결국 프롬프트 튜닝이라, "명시적 부정"과 "정보 없음"의 경계가 더 애매한
새로운 질문 유형에서는 재발할 수 있음 — 근본적으로 모델 능력의 한계이지 완전히 제거된
문제는 아님.

## 36. Router + interrupt() 기반 Human-in-the-loop 오케스트레이션 확장 (2026-07-24)

**원인**: 기존 오케스트레이터는 시장조사→PESTEL/경쟁사비교(병렬)→기획서초안까지 자동으로
끝까지 도는 고정 파이프라인이라, 초안을 본 사용자가 채팅으로 재작업을 요청하거나 질문할 수
있는 지점이 코드 구조상 없었음(`router_orchestrator_설계안.md` "B안" 채택 배경).

**과정**: `orchestrator/graph.py`에 신규 노드 7개를 추가함 — `await_review`(초안 완료 후
`interrupt()`로 정지), `router`(재개된 사용자 메시지를 구조화 출력 LLM 1회 호출로 판단해
`Command(goto=...)`로 분기), `research_revision`/`pestel_revision`/`competitor_revision`
(재작업 3종, 끝나면 항상 writer로 복귀), `capability_qa`(서비스 기능 질문 응답, 에이전트
재실행 없음), `finalize`(종료 처리). `agents/router.py`에 `decide_next_action()`(action
enum: approve/revise_market_research/revise_pestel/revise_competitor/capability_question/
unclear)을 신규 추가하고, 라우팅 오분류 비용이 크다고 판단해 `HEAVY_MODEL`(solar-pro2)을
씀. `PlanningState`에 `chat_history`/`revision_count`/`awaiting_user`/`pending_action_query`
필드를 추가. `revision_count >= REVISION_CAP(5)`면 router가 강제로 `finalize`로 보내
무한 재작업을 방지.

**결과**: 체크포인터는 설계안이 제안한 `AsyncSqliteSaver` 대신 동기 `SqliteSaver`를 채택함
(이유: 이 프로젝트의 모든 `agents/*.py`가 동기 함수라, 2주 MVP 막바지에 비동기를 앞당겨
들이면 모든 호출 경로에 async/await를 새로 얹어야 하는 위험이 큼 — FastAPI 도입 시점
(Task #7)에 자연스럽게 `AsyncSqliteSaver`+`ainvoke()`로 교체 예정). mock 검증
(MemorySaver + 가짜 LLM 응답)으로 4개 행동 경로 전부와 `REVISION_CAP` 도달 시 자동 종료까지
확인.

**의의**: "LLM이 몇 군데 박힌 자동화 스크립트"에서 "사용자와 대화하며 스스로 판단하는
에이전트 시스템"으로 성격이 바뀌는 지점 — 체크포인트①(유기적 Multi-Agent 수행)을 가장
강하게 입증함.

**한계**: mock 검증만으로는 실제 파일 기반 체크포인터의 프로세스 간 상태 지속성, 실제
LLM의 라우팅 판단 정확도까지는 확인할 수 없음 — 37·38절은 이 한계가 실제로 문제를 낸
사례임.

## 37. Task #4 실기기 end-to-end 검증 — 반복 실행 시 Fact Store 누적 문제 발견·수정 (2026-07-24)

**원인**: 36절 구현을 사용자가 실제로 "스마트 반려동물 건강관리 기기"(북미 반려동물 오너
시장) 주제로 처음부터 끝까지 실행함 — capability_question/revise_competitor/unclear/approve
4개 경로 전부 실제 LLM·웹검색으로 정상 동작하는 것을 확인(Task #4 실사용 검증 완료). 이
과정에서 사용자가 같은 topic으로 CLI를 중간에 다시 실행하면 매번 시장조사부터 재실행돼
Fact Store에 fact가 계속 쌓인다는 것을 지적함.

**과정**: 토이 그래프로 재현 테스트함 — LangGraph는 같은 `thread_id`라도 "이미 실행한 적
있으니 건너뛴다" 같은 자동 판단을 하지 않고, 새 초기 상태로 `invoke()`하면 체크포인트
이력과 무관하게 매번 START부터 그대로 다시 실행한다는 것을 확인함(`start_project()`는
항상 "새 프로젝트 시작"을 의미함).

**결과**: `_check_existing_session(thread_id)` 함수를 추가함 — `app.get_state(config)`로
기존에 `await_review`에서 멈춰있는 세션이 있는지 먼저 확인하고, 있으면 `start_project()`
호출 자체를 건너뛰고 바로 재개 루프로 들어감. CLI 진입점(`if __name__ == "__main__":`)에
반영. mock으로 재시작 시나리오를 흉내내 `research_node` 호출 횟수가 1회로 유지되는 것을
확인함.

**의의**: mock 테스트만으로는 드러나지 않았을 종류의 버그 — "여러 프로세스에 걸친 상태
지속성"은 실제 파일 기반 체크포인터로 실제 재실행을 해봐야만 드러남을 재확인함.

**한계**: 이 수정은 CLI 진입점에만 적용됨 — Task #7(FastAPI)에서는 API 계약 자체가 "새
프로젝트 시작"과 "기존 세션 재개"를 요청 경로(`POST /projects` vs
`POST /projects/{id}/messages`)로 구분하게 될 것이라 같은 문제가 원천적으로 발생하지 않을
전망이지만, 실제 구현 시점에 재확인이 필요함.

## 38. 체크포인터 msgpack 직렬화 경고 제거 (2026-07-24)

**원인**: 37절 실기기 테스트 중, `Fact`/`MarketSizing`/`Competitor` 등 Pydantic 모델과
관련 Enum이 그래프 state에 그대로 담기는데(1차 구현 때부터 있던 특성 — 체크포인터가 없어서
지금까지 드러나지 않았을 뿐), 체크포인터가 실제로 state를 직렬화하면서 "Deserializing
unregistered type ... This will be blocked in a future version" 경고가 뜨는 것을 발견함.

**과정**: `SqliteSaver.from_conn_string()`은 serde를 지정하는 인자를 노출하지 않는다는 것을
`inspect.getsource()`로 확인함. 직접 `sqlite3.connect()`로 연결을 열고
`SqliteSaver(conn, serde=...)`로 감싸는 방식으로 우회 가능하다는 것을 확인.

**결과**: `_open_checkpointer()` contextmanager를 신규 작성함 —
`JsonPlusSerializer(allowed_msgpack_modules=[...])`에 실제 프로젝트가 쓰는 6개 타입
(`SourceTier`/`VerificationStatus`/`Fact`/`MarketSizing`/`CompetitorType`/`Competitor`)만
정확히 나열해 허용 목록으로 등록함(전체 허용 대신 6개만 여는 이유: 이 허용 목록 자체가
"신뢰 안 되는 데이터의 역직렬화로 인한 임의 코드 실행 방지"라는 보안 목적을 가지고 있어서).
`start_project()`/`submit_message()`가 이 함수를 쓰도록 교체. 기존 mock 테스트를 재실행해
경고가 사라지는 것을 확인함.

**의의**: 보안 목적의 허용 목록 방식(전체 허용이 아니라 정확한 나열)을 택해, 편의성과
안전성을 동시에 확보함.

**한계**: 이 수정 이후 실제 LLM 세션으로는 아직 재확인하지 않음 — 다음 실제 실행 때 경고가
재발하지 않는지 한 번은 눈으로 확인할 필요가 있음.

## 39. 기획서 초안 v1/v2 비교분석 — 투자/매출 필드 분류 오류 발견·수정 (2026-07-24)

**계기**: 37절 실기기 테스트에서 사용자가 "경쟁사 정보 좀 더 찾아줘, 특히 가격과 투자/매출
정보를 더 검색해"라고 요청해 생성된 v2 DOCX를, 재검색 이전 v1 DOCX와 `pandoc`/`diff`로
정밀 비교함.

**결과 1) 개선 확인**: 가격 정보가 크게 보강됨. 경쟁사 재분류도 개선됨 — 마이벳이미징·
스카이라인 PACS가 이번엔 SKT 산하로 정확히 병합됨(23·25절에서 다루던 엔티티 중복 문제의
연장선). PESTEL·시장규모 섹션은 v1/v2 동일 — Router가 "요청한 것만 재작업"한다는 설계
의도가 실제로 지켜지는 것을 확인.

**결과 2) 새 버그 발견**: Whistle의 "2016년 Mars Petcare에 $117M 인수" 사실이 존재하고
올바른 경쟁사에 귀속됐는데도, DOCX 표의 "타깃·비고" 컬럼에는 나타나지 않음 — 원인을
추적한 결과 `funding_or_revenue`가 아니라 `strengths` 필드에 잘못 분류되어 저장된 것으로
확인.

**원인**: `agents/competitor.py`의 `PROFILE_SCHEMA`가 `funding_or_revenue`/`strengths` 두
필드의 구분 기준을 description에만 담고 있어, "피인수 사실이 시장에서 인정받았다는
의미로도 읽힐 수 있다"는 경계 사례에서 LLM이 정성적 강점으로 오분류함(35절과 동일한
유형의 문제).

**과정**: 35절에서 확인한 "description만으론 부족하다"는 교훈을 그대로 적용함 — 스키마
description과 `build_competitor_profiles()` 프롬프트 본문 양쪽에 "금액이 언급된 재무
사실은 무조건 `funding_or_revenue`" 규칙과 Whistle/Mars Petcare 구체 예시를 중복 기재.

**검증**: mock 배선 테스트로 (a) 반환값이 올바른 필드에 들어가는지, (b) 프롬프트에 새
규칙 문구가 실제로 포함되는지 확인함. 실제 LLM 순응도는 다음 실제 경쟁사 분석 실행에서
확인할 예정(강제 재테스트는 비용 문제로 하지 않기로 함).

**한계**: 이 수정을 제안했을 때 "Tavily 연동으로 해결되는 문제 아니냐"는 질문을 받았는데,
확인 결과 Tavily는 검색 단계(Reviewer 재검색 트리거, 페이지 본문 추출 실패 폴백)에만 좁게
적용하기로 확정돼 있어 이 버그(검색 이후 분류 단계의 문제)와는 무관함 — 검색 품질 개선과
분류 정확도는 별개 문제라는 것을 재확인함.

## 40. writer.py revision_note 파라미터 추가 — 문서·파일명 버전 표시 통일 (2026-07-24)

**원인**: 설계안 3-5절에서 정한 대로, 사용자가 여러 차례 되돌아와 재작업을 요청하는 게
Task #4의 핵심 시나리오인데, 문서 내용에는 이게 최초 초안인지 몇 차 수정본인지 표시가
없었음(파일명에만 `_v2`/`_v3` 접미사가 붙었으나, 이는 "디스크에 같은 이름 파일이 몇 개
있었는지"를 셀 뿐 실제 재작업 횟수와는 무관한 별개의 카운팅 체계였음).

**과정**: `agents/writer.py`의 `run_writer()`에 `revision_note: Optional[str] = None`
파라미터를 추가함. `orchestrator/graph.py`의 `writer_node()`가 `state["revision_count"]`를
보고 0이면 `None`(최초 초안), 1 이상이면 `f"{revision_count}차 수정본"`을 만들어 전달.
`agents/docx_export.py`의 `export_to_docx()`/`_add_cover()`에도 같은 값을 전달해 표지
정보 표에 "수정 차수" 행을 조건부로 추가.

**결과**: 사용자 제안으로 범위를 확장함 — 처음엔 문서 내용에만 반영할 계획이었으나,
"파일명에도 반영할 수 없냐"는 질문을 받고 `_versioned_base_name()`을 재설계함.
`revision_note`가 있으면 그 문구를 슬러그로 붙인 파일명(`..._1차_수정본.docx`)을 쓰고,
없으면(최초 초안) 기존처럼 접미사 없는 이름을 그대로 씀. 기존 `_v2`/`_v3` 방식은 이름이
우연히 겹치는 드문 경우에만 작동하는 안전장치로 용도를 좁혀 유지. mock 검증 4건(최초
초안 무표시·1차 수정본의 마크다운/파일명/DOCX 표지 3곳 반영·이름 충돌 안전장치·
`writer_node()`의 실제 변환 로직) 전부 통과.

**의의**: 파일명 버전과 문서 내용 버전이라는 두 개의 서로 다른 숫자 체계가 하나로 합쳐져,
어느 파일이 몇 차 수정본인지 파일을 열지 않고도 알 수 있게 됨.

**한계**: 이 로직 자체는 LLM 호출이 없는 순수 포매팅이라 mock 검증으로 충분하다고
판단했으나, 실제 상호작용 세션에서 재작업을 거쳐 파일이 실제로 이렇게 저장되는지는 아직
실기기로 확인하지 않았음.
