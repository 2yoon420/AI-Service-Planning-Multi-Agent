# 기획서 디자인 가이드 (docx_design.md)

이 문서는 사업 기획서 DOCX의 테마·형식 명세입니다. 이 가이드대로 작성하면 원본과 동일한 룩앤필의 문서가 나옵니다.
(참조 원본: 웨어러블_헬스케어_기기_북미_시니어_건강관리_기획서.docx)

> **2026-07-23 개정**: 이전 버전은 참고 docx를 사람이 눈으로 보고 요약한 프로즈였고, 실제 파일의
> OOXML(`word/document.xml`, `word/footer1.xml`)과 대조한 결과 여러 항목이 실제와 어긋나 있었다.
> 이번 개정은 XML 실측값과, 이후 사용자 피드백으로 추가 수정한 부분(정보 카드형 표, 숫자 압축 표기,
> 시사점 카드 정렬)까지 모두 반영해 실제 구현(`agents/docx_export.py`)과 정확히 일치시켰다. 바뀐 항목은
> 각 절에 **[수정]** 표시.

## 1. 문서 기본 설정
- 용지: A4 세로 (11906 × 16838 twips)
- 여백: 상/하 1200 twips (~2.1cm), 좌/우 1300 twips (~2.3cm)
- 기본 글꼴: **맑은 고딕** (ascii/eastAsia/hAnsi 모두), 기본 크기 10pt (sz 20 half-pt)
- **[수정] 푸터**: "{topic} — {target_market} 기획서 초안  |  {페이지번호}" — 문서제목까지 포함, 페이지 번호만이 아님.
  8pt(sz14→7pt로 축소 적용), 색 `8A9AA0`, 중앙 정렬. (이전 버전 "페이지 번호만"은 실제 파일과 다름 — 실제
  `word/footer1.xml`에 제목 텍스트 run이 페이지 필드 앞에 그대로 있음)
- **[수정] 표지 다음 페이지 나눔 없음**: 표지 블록 → 01 개요 → 02 표까지 강제 페이지브레이크 없이 자연스럽게
  이어진다(실제 파일에 `w:br type="page"` 자체가 0개). 콘텐츠가 흘러넘칠 때만 자동으로 다음 페이지로 넘어간다.
- 정렬: 본문 왼쪽 정렬. 가운데 정렬은 표의 숫자/뱃지 셀에만 제한적으로 사용

## 2. 색상 팔레트 (hex, # 없이 표기 시 w:color 값)
| 역할 | HEX | 용도 |
|---|---|---|
| Primary (딥 틸) | `0F4C5C` | 표지 대제목, 표 헤더 배경, H1/H2 제목 텍스트, 강조 본문 |
| Accent (틸) | `1B8A8F` | H1 번호·H2 마크, PESTEL 배지 배경, 카드 배경, 등급 텍스트, KPI 카드 상단 강조선 |
| Sub (그레이 블루) | `5A6B73` | 부제목, 각주성 문구(※…), 비고 셀, 출처 텍스트 |
| 본문 다크 | `222222` | 일반 본문/표 본문 |
| 흰색 | `FFFFFF` | 어두운 배경 위 텍스트 |
| 경고 텍스트 | `9A6B00` | 경고 콜아웃 텍스트 |
| 경고 배경 | `FBF6E9` | 경고 콜아웃 배경(shd fill) |
| 연한 틸 배경 | `E9F4F4` | 표지 메타 표 라벨 셀 배경 |
| 지브라/카드 배경 | `F5F8F8` | 표 짝수 행 배경, KPI/시사점 카드 셀 배경 |
| 경계선 | `C9DADB` | 모든 표의 상/하 얇은 선 |
| 푸터 회색 | `8A9AA0` | 푸터 텍스트 |

## 3. 표지 블록 (문서 첫 페이지 상단, 뒤이어 페이지 나눔 없이 01 개요로 바로 이어짐)
1. 라벨 줄: `사업 기획서 초안  |  DRAFT` — 9pt, Bold, `1B8A8F`. **[수정] 버전 표기(v0.2 등) 없음** — 자동 생성
   문서라 버전 번호를 특정할 근거가 없어 제외.
2. 대제목: 제품/사업명 — **26pt, Bold, `0F4C5C`**
3. 부제: 시장/목적 한 줄 — 15pt, `5A6B73`
4. **[수정] 문서 메타 표 = "미니멀 정보 카드" 스타일** (일반 격자표 아님):
   - 2열 표, 행 수 = 항목 개수(헤더 행 없음). 라벨 열 24% : 값 열 76% (twip 2200 : 7106, A4 기준)
   - **테두리**: 좌/우 완전히 없음(`w:left`/`w:right` = `val="nil"`). 각 행 위/아래에만 0.5pt(`sz=4`) 얇은 회색(`C9DADB`) 실선
   - **라벨 셀**: 배경 `E9F4F4`, 텍스트 Bold `0F4C5C` 9.5pt
   - **값 셀**: 배경 없음(흰색), 텍스트 일반 굵기 9.5pt
   - 셀 내부 여백: 상하 0.05in(72twip), 좌우 0.08in(115twip) / 세로 정렬 가운데
   - 행 예: 목표 시장 / 작성일 / 작성 (3행)
   - **구현 시 필수 사항** (안 지키면 기본 격자표로 되돌아감):
     1. `table.style = None` — Table Grid 등 기본 스타일 절대 사용 금지
     2. `w:tcBorders`의 `left`/`right`는 `val="nil"`로 명시 (sz=0만으로는 렌더러에 따라 안 지워질 수 있음)
     3. `w:tblGrid`/`w:tblW`를 표 생성 직후 명시적으로 다시 지정 — 안 하면 좁은 라벨 열(24%)이
        렌더러에 따라 무시되고 두 열이 균등폭으로 늘어남 (실제로 겪은 버그)
     4. 문단 `space_before`/`space_after`를 0으로 명시 — 기본 스타일 여백 때문에 행이 필요 이상으로 커 보임

## 4. 제목 체계
- **H1 (섹션 제목)**: `01  개요 (Executive Summary)` — 2자리 번호 + 공백 2칸 + 제목(+영문 병기).
  **[수정] 번호는 `1B8A8F`(accent), 제목 텍스트는 `0F4C5C`(primary)로 색이 분리됨** (전체 단색 아님).
  15pt, Bold. **[수정] 문단 하단에 accent 밑줄**(`w:pBdr bottom`, color=`1B8A8F`, sz=12, space=4) 있음.
  Word 스타일 Heading1.
- **H2 (일반 소제목)**: `▎ 가정 및 근거` — 앞에 `▎ ` 프리픽스. **[수정] 마크(▎)는 accent, 뒤 텍스트는
  primary로 색 분리** (전체 단색 아님). 12pt, Bold. 스타일 Heading2.
- **[수정] PESTEL 배지(카테고리 헤더)**: 문단 전체를 띠로 칠하는 게 아니라, **"P" 한 글자(앞뒤 공백 포함
  " P ")만 런(run) 단위로 `1B8A8F` 배경 + 흰색 Bold**. 그 뒤에 이어지는 `  Political — 정치적 환경`
  텍스트는 배경 없이 `0F4C5C`(primary) Bold. 11pt. Heading2 스타일의 한 문단 안에 두 런으로 구성.
- 본문 강조: 전체 문단을 bold 처리하는 게 아니라, **숫자·비율·개수 표현(예: `529억`, `17.6%`, `6가지`,
  `17개`)만 부분적으로 Bold+`0F4C5C`**로 강조하고 나머지는 일반 텍스트. (개요/경쟁구도종합 문단에 적용)
- 일반 본문: 10pt, `222222`.
- **[수정] 각주성 문구**: `※ 중요도가 낮아 요약에서 제외한 fact: N건` — 8pt, **이탤릭**, `5A6B73`.
  (이탤릭을 쓰지 않는다는 이전 버전의 규칙은 실제 파일과 반대 — 실제로는 이탤릭 사용)

## 5. 표 스타일 (모든 표 공통)
- **[수정] 테두리는 "전체 그리드"가 아니라 "좌우 없음, 상하만"** — 모든 데이터 셀은 개별적으로
  `top`/`bottom` = single 0.5pt(`sz=4`) `C9DADB`, `left`/`right` = `nil`. (표 레벨 `tblBorders`에는
  fallback으로 전체 그리드 auto를 남겨두지만 셀 단위 지정이 항상 이를 덮어써 시각적으로는 좌우선이
  전혀 보이지 않는 "잡지형" 스타일)
- 레이아웃: fixed, 표마다 twip 폭 합에 맞춰 `tblGrid`/`tblW`를 명시적으로 재지정
- 헤더가 있는 표는 `w:tblHeader`로 헤더 행 반복 지정 (여러 페이지로 넘어갈 때 헤더 유지)
- 셀 텍스트 기본 9.5pt

### 5-1. 데이터 표 (시장규모, 경쟁사, 출처 부록)
- 헤더 행: 배경 `0F4C5C`, 흰색 Bold 9.5pt
- 본문 행: **지브라 스트라이프** — 홀수 행 배경 없음, 짝수 행 `F5F8F8`
- 첫 열(구분/이름): `0F4C5C` Bold
- **[수정] 02 시장 규모 분석 데이터 표**(구분/규모/산정방식/정의, twip 1500/2300/1600/4106)는 KPI
  카드와 별개로 존재하는 표 — 이전 버전에는 이 표 자체가 명세에 없었음(불릿만 있었음). "규모" 열은
  §7의 압축 표기(억/만 단위)를 그대로 사용.
- 경쟁사 표 열 구성: 경쟁사(1650) | 가격(1700, 8.5pt) | 핵심 기능(4456, 헤드라인 `0F4C5C` Bold 9pt +
  나머지 기능 `222222` 8pt 두 줄) | 타깃·비고(1500, `5A6B73` 8pt)
- 출처 부록 표: 등급/판정(1150, `1B8A8F` Bold 7.5pt) | 수집 fact(5456, `222222` 7.5pt) | 출처
  URL+조회일(2700, `5A6B73` 6.5pt, 2줄)

### 5-2. TAM·SAM·SOM 카드 표 (1행 3열)
- 3열 균등(각 3102), 전 셀 배경 `F5F8F8`
- **[수정] 셀 테두리**: 위쪽만 accent 굵은 선(`1B8A8F`, sz=16≈2pt), 아래쪽 `C9DADB` 얇은 선, 좌우 없음
- 셀 내용 세로 구성: 라벨(TAM/SAM/SOM, `1B8A8F` Bold 9pt, 가운데 정렬) → 금액(`0F4C5C` Bold **15pt**,
  가운데 정렬) → 설명 한 줄(`5A6B73` **7.5pt**, 가운데 정렬)
- **[수정] 금액은 §7의 압축 표기(`$529.0억` 형식)를 사용** — 0이 많은 원 숫자를 그대로 쓰지 않음

### 5-3. 시사점 넘버 카드 (항목당 1행 2열 표를 반복)
- 1열(번호, 폭 560twip): 배경 `1B8A8F`, 흰색 Bold **13pt**, 가운데 정렬(수평·수직 모두)
- 2열(내용, 폭 8746twip): 배경 `F5F8F8`, 제목 줄 `0F4C5C` Bold 10pt + 본문 `222222` 9pt
- 카드 사이 빈 문단으로 간격
- **[수정] 구현 필수 사항**:
  1. 번호 열이 표 전체 폭의 6%(560/9306)에 불과할 정도로 좁으므로, §3-정보카드와 동일하게
     `tblGrid`/`tblW`를 명시적으로 재지정하지 않으면 렌더러가 균등폭으로 늘려버림
  2. 번호 셀·제목 셀·본문 셀 모두 문단 `space_before`/`space_after`를 0으로 지정 — 기본 여백이
     남아있으면 `vAlign=center`를 줘도 숫자가 셀 위쪽에 치우쳐 보임 (실제로 겪은 버그)

## 6. 콜아웃 (경고/미비 사항)
- 셀 배경 `FBF6E9`(1x1 표), 텍스트 `9A6B00` Bold 9.5pt
- 형식: `⚠ Bottom-up 미계산: …사유… 사용자 입력 시 재산정.` — ⚠ 프리픽스 사용

## 7. 숫자 표기 (압축 형식) — **[신규 추가]**
- 0이 많은 원 숫자(예: 52,900,000,000)를 그대로 쓰지 않고 **억/만 단위로 압축**한다.
  - 1억(1e8) 이상: 억 단위 소수 1자리 — 예: `529.0억`
  - 1만(1e4) 이상 1억 미만: 만 단위 정수 — 예: `9,216만`
  - 그 미만: 그대로 표기
- KPI 카드(§5-2)에서는 `$` 접두사만 붙임 — 예: `$529.0억`
- 데이터 표(§5-1)에서는 `$` 없이 단위 한글명을 뒤에 붙임 — 예: `529.0억 달러`
- 가정·근거 불릿(§8)의 "TAM=52,900,000,000 USD" 같은 원본 숫자는 이 규칙 대상이 아님(다른
  에이전트가 생성한 근거 텍스트를 그대로 인용하는 부분이라 재포맷하지 않음)

## 8. 리스트
- 가정·근거 등 핵심 불릿: ListParagraph 스타일. 앞부분(핵심값)만 Bold `0F4C5C`, 뒷부분(설명)은
  일반 텍스트 `222222` — 전체를 bold 처리하지 않음
- 형식: `TAM = 153.6억 달러  —  근거 설명…` (핵심값 — 긴 대시 — 설명) 또는 `[Top-down] TAM=... (설명)`
  형태면 첫 괄호 앞까지만 bold

## 9. 문서 구조 (섹션 순서와 구성 패턴)
1. **표지 블록** (라벨 → 대제목 → 부제 → 정보 카드형 메타 표) — 페이지 나눔 없이 바로 이어짐
2. **01 개요 (Executive Summary)** — TAM/SAM/SOM 카드 표 + 숫자 부분 강조된 리드 요약 문단
3. **02 시장 규모 분석 (TAM · SAM · SOM)** — 데이터 표(구분/규모/산정 방식/정의) + (10배 이상 괴리 시)
   ⚠ 콜아웃 + `▎ 가정 및 근거` 불릿
4. **03 PESTEL 환경분석** — P/E/S/T/E/L 각각 "P" 배지 + 본문 + (제외 fact 있으면) `※ ...` 이탤릭 각주
5. **04 경쟁사 분석** — `▎ 직접 경쟁자 / 간접 경쟁자·대체재 / 잠재 진입자` 표 3개(있는 유형만) →
   `※ 유통·투자매출 제외 안내` 각주 → `▎ 경쟁 구도 종합` 문단(숫자 부분 강조)
6. **05 종합 시사점** — LLM이 "숫자. 제목: 본문" 형식으로 반환한 걸 파싱해 번호 카드로 렌더링.
   파싱 실패 시 평문 폴백
7. **06 출처 부록** — 안내 문단(8.5pt `5A6B73`) + 등급/판정·fact·출처 URL 표(조회일 명기)

## 10. 톤 & 표기 규칙
- 문어체 보고서 톤("~입니다/~가 필요"), 숫자·단위는 §7 압축 표기 사용
- 영문 용어 병기: `개요 (Executive Summary)`, `Political — 정치적 환경`
- 구분자: 가운뎃점 `·`, 긴 대시 `—`, 세로바 `|` 활용
- 데이터 공백은 숨기지 않고 명시: "정보 없음", "계산 불가", "fact에 없어 제외" 등을 각주/셀에 표기
- 강조는 색+Bold가 기본이며, **각주(※)에 한해 이탤릭 사용**. 밑줄·이모지는 쓰지 않음
  (⚠, ※, ▎ 기호만 허용)

## 11. 구현 위치
이 명세는 `agents/docx_export.py` 한 파일에 그대로 구현되어 있다. 핵심 헬퍼:
`_set_cell_hv_borders`(좌우 nil, 상하 얇은 선), `_set_table_grid_widths`(tblGrid/tblW 명시 재지정),
`_shade_run`(PESTEL "P" 배지), `_fmt_compact`(§7 숫자 압축), `_add_h1`/`_add_h2`(색 분리 + 밑줄).
