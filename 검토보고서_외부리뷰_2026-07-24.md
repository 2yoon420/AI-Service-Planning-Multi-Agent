# 멀티에이전트 시스템 외부 검토 보고서

- **작성일**: 2026-07-24
- **검토 범위**: 기준 문서 3종(`AI서비스기획보조_MultiAgent_통합기획문서.md` → `시장조사-PESTEL-경쟁사분석-파이프라인.md` → `router_orchestrator_설계안.md`) + `multi-agent-system/` 전체 코드 + 동료 프로젝트 team07-sequel(테스트·실험 체계)
- **목적**: 별도 세션(다른 계정 Claude)과 진행 중인 작업의 외부 검토. 이 문서는 작업 재개 시 컨텍스트 파일로 활용 가능하도록 작성함.

## 요약 (TL;DR)

1. 설계 문서와 코드의 일치도가 매우 높고, "발견→원인→수정→검증→한계" 문서화 문화가 이 프로젝트의 최대 자산이다.
2. router_orchestrator_설계안은 "계획 문서"로 표기돼 있으나 실행 순서 1~5번이 이미 구현 완료됐다(FastAPI만 미착수).
3. 가장 큰 빈틈은 **자동화된 회귀 테스트 부재** — 33개 절에서 고친 버그들이 다시 깨져도 알 수 없는 상태.
4. 동료 프로젝트 team07-sequel(동일 스택: LangGraph+Solar)의 3층 테스트 체계(pytest / 회귀 라벨셋 / 벤치 하네스)가 그대로 이식 가능한 템플릿이다.
5. gold SQL 같은 단일 정답 데이터는 우리 태스크에 없지만, 검증 부품별로는 공개 라벨 데이터(KLUE-NLI, RAGTruth, MIRACL 등)로 judge 교정이 가능하다.

---

## 1. 프로젝트 이해 (발표 요약)

### 1-1. 한 줄 정의와 차별점

주제·목표시장 입력만으로 시장조사→PESTEL→경쟁사비교→기획서 초안을 자동 생성하되, **모든 문장의 근거(fact)와 출처를 추적 가능하게 만드는** LangGraph 기반 멀티에이전트 시스템. 차별점은 결과물 품질이 아니라 **근거 추적성**(Manus류 서비스의 블랙박스 한계 보완)에 있으며, 이 관점이 문서와 코드 전체를 관통한다.

### 1-2. 세 문서의 진화

| 문서 | 성격 | 핵심 |
|---|---|---|
| 통합기획문서 | 무엇을 만들 것인가 | PRD 5개 에이전트 + Fact Store·Human Checkpoint·TAM/SAM/SOM 이중검증 |
| 파이프라인 문서 (797줄) | 만들면서 무엇이 깨졌는가 | recall↔precision 시소의 반복 발견과 구조적 대응 기록 |
| router 설계안 | 스크립트에서 에이전트로 | interrupt() + Command(goto) + checkpointer 기반 Human-in-the-loop |

파이프라인 문서의 버그 연쇄는 하나의 패턴으로 요약된다: **경쟁사 놓침(11-2, recall) → FDA 오식별(11-5, precision) → recall 개선(12) → 무관 fact 오염(12-5, precision) → 검증 계층 도입(13, CRAG/MAIN-RAG) → 정상 fact 오기각(13-8) → 프롬프트 보정(13-9)**. 프롬프트 땜질의 한계를 인식하고 논문 기반 검증 계층으로 구조화한 13절이 프로젝트의 전환점.

### 1-3. 구현 상태 대조 (2026-07-24 코드 기준)

| 설계안 항목 | 상태 |
|---|---|
| Fact Store topic 스코핑 (3-1) | ✅ 복합키 마이그레이션 + `_infer_legacy_topic()` (`fact_store/store.py`) |
| `run_targeted_research()` (3-4) | ✅ `agents/market_research.py:482` |
| capability RAG (3-6) | ✅ `agents/router.py` + 전용 컬렉션 분리 |
| 그래프 확장 (3-2/3-3) | ✅ `orchestrator/graph.py` 12노드, 옵션 B(구조화 단일 판단), 동기 SqliteSaver로 현장 조정 |
| `revision_note` (3-5) | ✅ writer 연동 + 파일명 버저닝 |
| FastAPI (3-7) | ❌ 미착수 (Task #7) |

설계를 그대로 따르지 않고 현장 판단으로 조정한 지점(AsyncSqliteSaver→동기 SqliteSaver, msgpack 허용 목록 6개 명시)에 이유가 주석으로 남아 있음 — 문서-코드 정합성 우수.

### 1-4. 강점 3가지 (강화 방향 포함)

1. **문서화 문화**: 33개 절 전부가 발견→원인→수정→검증→한계 구조. 발표에서는 기능 목록이 아니라 "실패의 연쇄와 구조적 대응"을 서사로 쓸 것.
2. **"LLM은 판단만, 산술·검증은 코드가" 원칙**: TAM/SAM/SOM 곱셈 코드 전담, 출처 조작 후처리 탐지, 프롬프트+코드 이중 안전장치 6곳 이상 반복 — 설계 철학으로 정착.
3. **근거 기반 의사결정**: 채점 척도의 근거 부재를 스스로 발견해 논문(G-Eval, Grading Scale Impact)으로 보강, CRAG/MAIN-RAG를 2주 MVP 제약에 맞게 의도적 축소 채택. "왜 이 방식인가"의 답이 준비돼 있음.

---

## 2. 보완점 상세 (우선순위순)

### ① 자동화된 회귀 테스트 부재 — 최우선

**원인(왜 문제인가)**: 테스트 파일은 API 연결 확인용 `test_connection.py` 하나뿐. 지금까지의 검증은 전부 "py_compile + 임시 스크립트 1회 + 사용자 로컬 재실행"의 1회성. 이미 검증된 순수 함수가 12개 이상 있으나(`_is_no_info_statement`, `_flag_fabricated_citations`, `_canonical_company_name`, `find_similar_fact`, `_bin_pack_axis_blocks`, `chunk_text`, `compute_adaptive_threshold`, `classify_source_tier`, `select_top_competitors`, topic 마이그레이션 등) 그 검증 스크립트가 남아있지 않다. 회귀 위험은 이미 실현된 적 있음 — 22절에서 `build_competitor_profiles()` 배치를 고치고 같은 구조의 `identify_competitors()`를 놓쳐 동일 에러 재발. 테스트는 리팩토링(⑤)의 전제조건이기도 하다.

**방법**: 3계층 — (a) 순수 함수 pytest(반나절, LLM 불필요, `tmp_path` 임시 SQLite), (b) mock LLM 통합 테스트(반나절, 25절의 mock 클라이언트 전례 재사용, JSON 절단·스키마 밖 enum 등 방어 로직 검증), (c) 실 LLM 스모크(`@pytest.mark.llm` 마커 분리). 이미 수행한 검증 시나리오를 그대로 코드화하면 됨(예: 10절 "84억 4천만 달러이다/입니다" 중복 판정 케이스).

**장점**: 비용 대비 효과 최대(기존 검증 재활용), 결정적·고속, 테스트가 실행 가능한 문서 역할. **단점**: 작성 1일, mock은 프롬프트의 실제 효과 검증 불가, 테스트 자체도 유지보수 대상. **의의**: "수정→재검증" 사이클을 기계화 — 문서화 문화의 코드 수준 완결. **한계**: 단위 테스트는 LLM 행동 변화를 못 잡음(→② 골든셋의 영역).

### ② Router 분류 정확도 골든셋

**원인**: `decide_next_action()`은 6-way 분류인데 정확도를 측정할 테스트셋이 없다. 오분류 비용이 비대칭적으로 큼(엉뚱한 재실행 = 웹검색+LLM 다수 호출 + REVISION_CAP 1회 소진). 설계안 3-3절에서 옵션 B를 택한 근거가 "테스트 용이성"이었으나 정작 테스트가 없음. HEAVY_MODEL 선택도 "직접 실측 비교는 안 했음" 상태.

**방법**: 행동 6종 × 5~8발화 = 30~50쌍. 반드시 3종 포함 — 명확 케이스, 경계 케이스("PESTEL에 최신 규제 반영해줘" → pestel인가 research인가; 코드 주석의 "재검색 필요 시 research로 먼저" 규칙이 검증 대상), 모호 발화(unclear가 정답). 같은 발화 5~10회 반복으로 판정 안정성도 측정(τ-bench의 pass^k 개념 — 1회 성공률만 보면 신뢰성 과대평가). 정확도 + 혼동 행렬 리포트.

**장점**: 프롬프트 수정 효과를 수치로 확인, mini vs pro2 실측 비교 가능. **단점**: 자기편향(개발자 발화 ≠ 실사용자 발화), LLM 호출 비용. **의의**: "에이전트로의 전환" 지점인 Router의 신뢰도 최초 수치화. **한계**: 골든셋 100%여도 실사용 발화 분포는 파일럿에서만 확보됨.

### ③ 검증 판정 "애매" 쏠림 — min 결합 재설계는 측정 이후에

**원인**: `min(관련성, 근거지지도)` 결합에서 근거지지도가 요약·재구성 특성상 3점에 캡핑되어(13-9), 3단계 판정이 사실상 2단계로 퇴화. NSM이 "출처검증 통과 기획서 수"인데 '통과(채택)'가 거의 발생하지 않아 지표 체계의 전제가 무너진 상태.

**방법(순서 중요)**: 가중합(0.6/0.4)을 바로 적용하지 말 것 — 근거 없는 min을 근거 없는 가중합으로 바꾸는 것뿐(5절에서 스스로 파헤친 "임의 설정" 함정의 반복). 대신: (1) fact 50개 사람 라벨링(1~2시간) → (2) 현재 min 방식의 사람-시스템 일치도 측정(기준선) → (3) 대안(가중합, 근거지지도 기준 명확화: "사소한 재구성=5점, 검증 불가능한 새 수치 추가만 감점")을 같은 라벨셋으로 비교 후 채택. LLM-as-judge는 사람 정렬도 실측이 표준(G-Eval의 Spearman 상관, MT-Bench의 judge 편향 보고). 관련성/근거지지도 분리 채점 구조는 RAGAS의 relevancy/faithfulness 분해와 동일 — 결합 방식 설계 시 직접 참고 가능.

**장점**: 3단계 confidence 복원 → Writer가 채택 fact 우선 인용하는 차등 대우 가능. **단점**: 라벨링 비용, 1인 라벨 주관성, Solar Mini 판단력 상한은 결합 방식 변경으로 안 사라짐. **의의**: "채점 기준의 근거 추적" 정체성을 결합 방식까지 확장. **한계**: 50개는 방향 확인용(통계적 확증 불가)임을 명시할 것.

### ④ PRD Must "재검색 루프 최대 2회" — 구현보다 재정의 권장

**원인**: PRD Must와 구현(기각/애매 표시까지만, 13-6에서 의도적 보류) 불일치. 심사에서 가장 쉽게 지적받는 유형.

**방법**: (a) 재정의(1시간, 권장) — "자동 재검색 루프를 Router 기반 Human-in-the-loop 재검색으로 발전시켰다"고 PRD·발표에 명시. 실제로 `run_targeted_research()`가 표적 재검색을 수행하며, 무엇이 부족한지의 판단 주체가 LLM→사람으로 바뀐 것은 Human Checkpoint 철학의 논리적 귀결. 단, 설득력을 위해 **기각/애매 fact 통계를 초안 문서에 노출**할 것(재검색 필요성의 판단 재료 제공). (b) 최소 구현(반나절) — 기각 비율 임계 초과 시 `simplify_query()` 재검색 1회(기존 부품 조립, CRAG corrective 원형).

**장점**: (a)는 비용 거의 없이 문서 정합성 회복, 약점을 설계 진화 서사로 전환. **단점**: (a)는 완전 자동화 포기 선언(가치 제안 일부 약화), (b)는 비용·무한루프 관리 부담. **의의**: "LLM 자동 판단은 완화는 가능하나 보장은 불가"라는 이 프로젝트 전체 교훈의 귀결로 제시 가능. **한계**: 사용자가 통계를 보고도 재검색을 안 하면 품질 미달 초안이 최종본이 됨(HITL의 본질적 한계).

### ⑤ 하드코딩 목록 6종 — 당장 유지, 통합은 테스트 이후

**원인**: `KNOWN_RESEARCH_FIRMS`, `NO_INFO_PATTERNS`, `NON_COMPETITOR_ENTITY_PATTERNS`, `PRODUCT_TO_COMPANY_MAP`, `KNOWN_COMPETITOR_BRANDS`, `_ENGLISH_REVIEW_MARKET_KEYWORDS` — 전부 "웨어러블/북미" 실행 관찰로 만들어짐. **시스템은 주제 무관을 표방하는데 안전장치는 주제 특화** — 다른 도메인 실행 시 경고 없이 품질 저하(최악의 실패 양식).

**방법**: MVP 이후. 25절 `_consolidate_duplicate_competitors()`(하드코딩 사전 → LLM 통합 패스 + 코드 안전장치)가 전례. (1) 엔티티 타입 분류(회사/기관/제품) LLM 판정으로 목록 2종 흡수, (2) 출처 실존 검증은 "인용 출처가 Fact Store에 실제 존재하는가" 대조(8-2-1 보강방향에 이미 기록됨)로 대체. 현실적으로 하이브리드(목록=비용 0 빠른 경로, LLM=폴백)가 정답. **선행조건: ①의 테스트로 기존 목록 케이스를 박제한 뒤 교체.**

**장단점·의의·한계**: 도메인 이전 가능성 확보(서비스 확장의 최소 조건) vs LLM 판정의 비용·비결정성·회귀 위험. 목록의 한계를 LLM의 한계로 교환하는 것이지 제거가 아님.

### ⑥ 소소한 정리

- `requirements.txt`의 미사용 의존성(`google-generativeai`, `anthropic`) 정리 또는 "향후 fallback용" 주석.
- capability_qa는 `revision_count`를 올리지 않아 REVISION_CAP에 안 걸림 — CLI에선 무해하나 FastAPI 공개 시 무제한 LLM 비용. Task #7에서 별도 상한(qa_count) 필요.

### 실행 순서 제안

④(a) 재정의(1h) → ①1계층 순수 함수 테스트(반나절) → ② Router 골든셋(반나절) → ③ 라벨링+측정(1~2h; 결합 변경은 측정 후) → ①2계층 mock(여유 시) → ⑤⑥ MVP 이후.

---

## 3. "측정용 라벨" 개념 정리 (Q&A에서 확정)

**질문 요지**: fact는 실행마다 웹에서 새로 수집되고, 향후 기획서별 별도 fact store 구상도 있는데, fine-tuning도 임베딩 학습도 아닌 라벨링이 의미가 있는가?

**답**: 라벨에는 두 용도가 있고, 여기서 제안하는 것은 측정용이다.

| | 학습용 라벨 | 측정용 라벨 (제안) |
|---|---|---|
| 소비 주체 | 모델 (가중치 변경) | 개발자 (품질 측정) |
| 필요 수량 | 수천~수만 | 수십(방향 확인) |
| 데이터가 바뀌면 | 다시 제작 | 그대로 유효 |
| 업계 용어 | training set | golden set / eval set |

핵심: 측정 대상은 fact가 아니라 **모든 fact가 통과하는 고정 부품**(grade_fact 프롬프트 2종 + Solar Mini 채점 + min 결합 + adaptive threshold)이다. fact는 시험지의 지문일 뿐. 온도계 교정에 쓴 물은 버려도 "온도계가 정확하다"는 사실은 남는 것과 같다. 13-8절에서 "누가 봐도 부합하는 fact가 1점 기각됐다"고 판단한 것 자체가 이미 라벨링이었음 — 제안은 그 눈대중 1회성 판단을 고정셋으로 박제하자는 것.

**프로젝트별 fact store 구상과의 관계**: store가 분리될수록 검증기는 모든 프로젝트가 공유하는 전역 부품이 되므로 측정 가치가 오히려 커진다. 라벨셋은 live store가 아니라 `tests/fixtures/verification_golden.json` 같은 동결 파일로 코드베이스에 보관 — store 생성/삭제와 무관하게 존속.

**유효한 반론(분포 이동)**: 웨어러블 fact 50개로 잰 정렬도가 타 도메인에 그대로 이전된다는 보장은 없음. 완화: 도메인 내용이 아니라 **실패 유형**(조작 출처 8-2-1 / 정보부재 메타발언 8-3 / 지역·카테고리 불일치 12-5 / 요약을 조작으로 오인 13-8 / 무근거 수치 추가 13-9) 중심으로 샘플링 — 유형은 도메인 초월적. 웨어러블+반려동물 두 실행분에서 섞어 뽑을 것.

---

## 4. team07-sequel 테스트·실험 체계 분석

### 4-1. 왜 특별히 가치 있는 참조인가

한국어 Text-to-SQL 서비스(Sequel). **기술 스택이 우리와 동일**: LangGraph + Upstage Solar(pro2 생성/mini 분류) + FastAPI. "약한 백본(Solar)을 엔지니어링과 측정으로 어디까지 보완하나"라는 문제의식도 동일. 여기서 검증된 방법은 같은 제약 조건에서 이미 작동한 방법이다.

### 4-2. 3층 테스트 체계

| 층위 | 위치 | 답하는 질문 | 핵심 관례 |
|---|---|---|---|
| pytest 단위/회귀 | `backend/tests/`, `tests/` | 코드가 깨졌나? | **버그 이력을 테스트 주석으로 박제**("2026-07-10 실사용 중 발견된 버그: WITH(CTE) 오탐 차단"), monkeypatch로 외부 API 차단, parametrize로 위험 케이스 일괄 |
| 회귀 라벨셋 | `tests/eval_linker.py` | 컴포넌트 품질 퇴보? | "이 라벨셋이 회귀 테스트셋이다" — 찾아야 할 것(SCHEMA_LABELS)+맞혀야 할 것(VALUE_LABELS)+**거부해야 할 것(CONCEPT_LABELS)** 3종 구성. 오답도 라벨링 |
| 벤치 하네스 | `bench/` (22 스크립트) | 설계 선택이 옳았나? | 평가셋 빌드 → **채점기 self-test(pred=gold→100% 확인)** → 실험 → jsonl 보존 → 요약 |

### 4-3. 실험 방법론의 백미 5가지 (`docs/실험_총정리_v2.md`)

1. **실험 카드 4줄 양식 통일**: 셋업(데이터·표본·모델·채점·원자료 파일명)/왜/결과/결론.
2. **시간순 원본과 읽기용 재편집 분리**: 총정리(시간순) + v2(기승전결). 우리 파이프라인 문서 33개 절은 "시간순 원본" 단계 — 발표 전 v2 재편집 필요.
3. **"스스로 깐다" 한계 섹션**: 표본 크기별 "유의하려면 필요한 격차" 표로 자기 결론을 유의/노이즈로 등급화(n=100이면 ±12pp 미만은 노이즈, n=25는 셀 해석 불가). few-shot 누수 수치를 "리더보드용 36~40%"와 "제품 맥락 51.8%"로 분리 선언. 오버피팅(튜닝셋=평가셋)·미측정 컴포넌트·재현성 구멍까지 자인.
4. **실험 인프라도 버그가 있다는 인식**: 병렬 실행이 레이트리밋 오염 유발 → 1차 결과 통째 폐기 → 병렬도 제한+에러행 재개를 벤치 표준화(실험 7).
5. **최적화 간 상호작용 회귀**: M-Schema로 스키마가 커지자 router의 `schema[:1500]` 하드 절단과 상호작용해 정상 질문 오차단(실험 12-회귀) — 우리 22절(배치 수정 누락)과 동일 패턴. 이런 상호작용은 회귀 테스트만이 잡는다.

### 4-4. 두 프로젝트의 수렴 (발표용 교차 인용 포인트)

그들의 가장 견고한 발견(실험 9, n=1,200): 불확실한 값 힌트 주입 시 −17.8pp, 어떤 프레이밍으로도 −9.4pp — **"확실한 것만 넣어라"**. 우리의 12-5(무관 fact 오염)→13(확실한 청크만 남기는 검증 계층)과 동일 원리의 독립적 재발견. 서로 다른 태스크·동일 백본에서 같은 교훈 — 발표에서 교차 인용 가치.

### 4-5. 즉시 이식 목록 (발표 전, 총 1.5~2일)

1. **pytest 골격+버그 박제 관례**(반나절): `pytest.ini`(pythonpath=./testpaths=tests) 이식, 33개 절 수정을 테스트화하며 각 테스트에 "몇 절, 언제 발견" 주석.
2. **eval_linker 스타일 3종 라벨셋**(반나절): 검증 에이전트용(반드시 채택/반드시 기각/애매가 정답), Router용(action별+경계+unclear가 정답).
3. **judge self-test**(1시간): 명백한 채택 케이스(원문 그대로 fact)와 명백한 기각 케이스(완전 조작 fact)를 `grade_fact()`에 통과 — 극단 케이스 100%가 안 되면 미세 튜닝 무의미.
4. **결과 jsonl 보존**(즉시): 실행별 지표(fact 수, 판정 분포, 경쟁사 수, 공란율, 시간·비용)를 append — 콘솔 로그만으로는 증거가 소멸.
5. **실험 카드 양식으로 v2 재편집**(발표 준비 겸): 기(출처 조작 발견)→승(검증 레버 탐색)→전(recall↔precision 시소)→결(검증 계층+Router 확정).

### 4-6. 구조적 차이 — 그대로 못 가져오는 것

그들에게는 **gold SQL**이 있어 실행 결과 비교(EX)라는 객관·자동·결정적 채점으로 1,200문항 벤치마크가 성립했다. 우리 산출물(기획서)에는 gold가 없다. 대응 3단계: (a) gold 없이 객관 측정 가능한 **구조 지표**(완주율, fact 수집량, 판정 분포, 프로필 공란율, 시간·비용) → (b) 소규모 **사람 라벨 정렬도**(2장 ③) → (c) LLM-as-judge(단, judge를 (b)로 교정한 뒤). 업계 가이드도 에이전트 평가를 최종 출력과 경로(trajectory)·컴포넌트 단위로 분리하라고 권함.

### 4-7. 우리가 돌릴 실험 제안 (우선순위순)

| 실험 | 질문 | 방법 | 표본·비용 |
|---|---|---|---|
| A. judge 정렬도 | min 결합이 사람 판단과 일치? | 사람 라벨 50 → min vs 가중합 | 라벨 1~2h |
| B. Router 정확도 | 6-way 분류 정확도·안정성 | 골든셋 30~50 × 반복 5회(pass^k) | LLM 150~250콜 |
| C. 관련성 필터 ablation | 12-5 수정 효과 수치화 | 같은 topic, on/off 오염 fact 비율 | 실행 2회 |
| D. 청크 필터링 ablation | 13절 검증 계층 기여도 | on/off 기각률·품질 비교 | 실행 2회 |
| E. 청크 크기 | 7-2절 "실험 안 함" 자인 항목 | 400/800/1200자 검색 정확도 | 소규모 |

주의(그들의 검정력 교훈): 우리 실험은 n이 작아 대부분 **"방향 확인용"이지 확정이 아님** — 숨기지 말고 명시. fact 단위 지표에는 부트스트랩 신뢰구간(재표집 500~1,000회) 권장 — LLM 평가는 오차 구조가 비정규라 부트스트랩이 표준 권장.

---

## 5. 공개 라벨 데이터셋 활용 — "gold SQL의 부분 대체물"

최종 산출물용 gold는 없지만, 검증 파이프라인의 부품별로는 공개 라벨 데이터가 존재한다. 우리 채점 관점 2개(관련성/근거지지도)가 학계 표준 태스크와 구조가 같기 때문.

### 5-1. 컴포넌트별 매핑

| 우리 컴포넌트 | 표준 태스크 | 데이터셋 | 언어 |
|---|---|---|---|
| 근거지지도 (`grade_fact`) | NLI | **KLUE-NLI**(~3만 쌍), KorNLI | **한국어** |
| 근거지지도+환각 유형 | RAG 환각 탐지 | **RAGTruth**(1.8만, 단어 수준), RAGBench | 영어 |
| 관련성 (`filter_relevant_chunks`) | IR 관련성 | MIRACL, Mr.TyDi | 한국어 포함 |
| 사실 검증 구조 | claim verification | FEVER(18.5만), AVeriTeC | 영어 |
| 엔티티 타입(11-5 FDA 문제) | NER | KLUE-NER(ORG 라벨) | 한국어 |
| PESTEL 축 태깅 | 뉴스 주제 분류 | KLUE-TC(YNAT) | 한국어(약한 프록시) |
| Router 6-way | — | 없음, 자체 구축 | — |
| TAM/SAM/SOM·기획서 품질 | — | 없음(유료 리서치 영역) | — |

### 5-2. 핵심 2종 활용법

**KLUE-NLI — 근거지지도 judge의 무료 대량 정답지.** NLI(전제가 가설을 함의하는가: entailment/contradiction/neutral)는 우리의 "원문이 fact를 뒷받침하는가"와 동일 구조. 자연 발생 한국어 문장 기반이라 번역체 KorNLI보다 라벨 일치도가 높다고 보고됨. 활용: 300~500쌍을 `grade_fact()` 근거지지도 프롬프트에 통과시켜 entailment→고득점/contradiction→저득점 비율 측정 — **라벨링 0시간으로 수백 개 규모 judge 교정**(자체 50개로는 불가능한 표본).

**RAGTruth — 실패 유형 분류체계의 검증된 원형.** RAG 응답 1.8만 건에 단어 수준 환각 스팬 라벨. 4유형(명백한 모순/미묘한 모순/명백한 무근거/**미묘한 무근거**) 중 마지막이 우리 13-9의 경계 문제(요약·재구성 vs 조작)와 정확히 대응. 영어라 직접 채점보다는 **자체 골든셋 라벨링 기준(taxonomy)으로 차용** — "임의 기준"이 아니라 "공인 벤치마크 정합 기준"이 됨.

보조: AI Hub(team07이 NL2SQL에 사용)에 한국어 문서요약 계열 데이터셋 다수 — 요약 충실성 프록시. 무료, 한국 계정 로그인·승인 필요.

### 5-3. 한계와 2층 하이브리드 전략

한계: (1) 도메인 이동 — 위키·뉴스 문장 ≠ 시장조사 웹문서. 공개셋 통과는 필요조건(범용 능력)이지 충분조건 아님. (2) 태스크 근사 간극 — NLI의 neutral ≠ 우리의 "애매". (3) 파이프라인 전체의 단일 자동 지표(EX 등가물)는 끝내 없음.

전략: **공개셋(KLUE-NLI 수백 쌍)으로 범용 능력을 대량·무료 교정 + 자체 골든셋(50개, RAGTruth 유형 라벨)으로 도메인 정렬 확인.** 공개셋에서 실패 → 프롬프트/모델(Mini→Pro2) 문제. 공개셋 통과 + 자체셋 실패 → 도메인 특화 문제. **어느 층에서 실패하는지가 곧 원인 진단.**

---

## 6. 종합 로드맵 (남은 기간 기준)

| 순서 | 작업 | 소요 | 근거 |
|---|---|---|---|
| 1 | ④(a) PRD 재검색 루프 재정의 + 기각/애매 통계 초안 노출 | 1h+α | 문서 정합성, 최저 비용 |
| 2 | ① 1계층: 순수 함수 pytest (버그 박제 주석 포함) | 반나절 | 리팩토링·회귀 안전망 |
| 3 | ② Router 골든셋 + pass^k 반복 | 반나절 | 오분류 비용 최대 지점 |
| 4 | judge self-test (극단 케이스 sanity) | 1h | 미세 튜닝의 전제 |
| 5 | ③ fact 50개 라벨링 → min 기준선 측정 | 1~2h | 결합 방식 변경의 전제 |
| 6 | 결과 jsonl 보존 로직 | 1~2h | 실행 이력 데이터화 |
| 7 | 33절 문서의 "실험 카드 v2" 재편집 | 발표 준비 겸 | team07 양식 |
| 8 | KLUE-NLI 수백 쌍 judge 교정 | 여유 시 | 무료 대량 검증 |
| MVP 후 | ⑤ 하드코딩 목록 통합, ⑥ 정리, FastAPI(Task #7), 실험 C/D/E | — | 테스트 안전망 확보 후 |

---

## 7. 참고 자료

**프로젝트 내부 근거**: 통합기획문서, 파이프라인 문서(특히 5, 8-2-1, 11-2~11-5, 12-5, 13, 13-8, 13-9, 22, 25절), router_orchestrator_설계안, `orchestrator/graph.py`, `agents/router.py`, `fact_store/schema.py·store.py`

**team07-sequel**: [repo](https://github.com/Doyunamic-Kwon/edu-gachon2026-project/tree/main/projects/team07-sequel) — `docs/실험_총정리_v2.md`, `tests/eval_linker.py`, `backend/tests/test_guardrail.py`, `tests/test_metrics.py`, `bench/evaluate.py`, `bench/README.md`

**논문**:
- G-Eval: NLG Evaluation using GPT-4 (arXiv:2303.16634) — LLM judge의 사람 정렬도 측정 방법론
- Grading Scale Impact on LLM-as-a-Judge (arXiv:2601.03444) — 0-5 척도 정렬도 최고 (프로젝트 기존 인용)
- Judging LLM-as-a-Judge with MT-Bench (arXiv:2306.05685) — judge 편향(위치·장황함·자기강화), 정렬도 실측 필요
- RAGAS: Automated Evaluation of RAG (arXiv:2309.15217) — relevancy/faithfulness 분리 채점
- τ-bench (arXiv:2406.12045) — pass^k 반복 신뢰도 지표
- AI Agents That Matter (arXiv:2407.01502) — 벤치마크와 실사용 신뢰성 간극, 비용 통제 평가
- CRAG (arXiv:2401.15884), MAIN-RAG (arXiv:2501.00332) — 프로젝트 기존 채택 근거
- KLUE (arXiv:2105.09680) — 한국어 NLI/NER/TC
- RAGBench (arXiv:2407.11005) — 설명 가능한 RAG 벤치마크

**웹 자료**:
- [DeepEval — LLM-as-a-Judge 기법·pytest 통합](https://deepeval.com/blog/llm-as-a-judge)
- [FutureAGI — LLM 회귀 테스트 가이드](https://futureagi.com/glossary/llm-regression-testing/)
- [QASkills — 골든 데이터셋 구축 가이드](https://qaskills.sh/blog/golden-dataset-llm-evaluation-guide)
- [Confident AI — 에이전트 평가 지표 2026](https://www.confident-ai.com/blog/llm-agent-evaluation-complete-guide)
- [Digital Applied — 에이전트 평가 파이프라인 방법론](https://www.digitalapplied.com/blog/ai-agent-evaluation-pipeline-2026-testing-methodology)
- [Indeed Engineering — LLM 평가 부트스트랩 신뢰구간](https://engineering.indeedblog.com/blog/2026/07/bootstrap-confidence-intervals-for-llm-evaluation/)
- [Multi-Agent Routing as Set-Valued Prediction (arXiv:2606.28925)](https://arxiv.org/pdf/2606.28925)
- [Evidently AI — RAG 벤치마크 7종](https://www.evidentlyai.com/blog/rag-benchmarks)
- [한국어 NLI 데이터셋 모음 (GitHub)](https://github.com/oikosohn/korean-nli-datasets)

---

*본 보고서는 2026-07-24 외부 검토 세션(Claude)에서 작성됨. 기준 문서 3종·코드 전체·team07-sequel repo를 직접 확인한 내용에 근거하며, 웹 출처는 상기 링크 참조.*
